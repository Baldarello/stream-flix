# graphify semantic-graph rebuild key.
# Copy this file to `graphify.env` and set a real value before running
# `graphify extract .` (or any command that triggers a semantic LLM pass).
#
# Get a key at: https://aistudio.google.com/apikey
GEMINI_API_KEY=
