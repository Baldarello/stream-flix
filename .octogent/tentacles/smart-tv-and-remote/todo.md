# Todo
- [ ] Create `frontend/features/tv/screens/TvHomeView.jsx` as minimal stub view that imports `tvStore` from `frontend/store/tvStore.js`, renders a `<div id="tv-home-view">` with TvHomeView title and a "QR Pairing" button calling `tvStore.requestPairing()` (file missing on disk; `frontend/features/tv/screens/TvScreenRouter.jsx:4` imports it, TV mode crashes at boot)
- [ ] In `frontend/features/tv/screens/TvPlayerView.jsx` around line 90-93 remove `tvStore.navigate('home')` from render body and move it into a new `useEffect` that fires when `!mediaStore.nowPlayingItem && isActive` so navigation happens after render not during
- [ ] In `frontend/features/tv/screens/TvMyListView.jsx` lines 48 and 67 add `type="button"` to both `<button>` elements to prevent accidental form submit default
- [ ] In `frontend/features/tv/screens/TvMyListView.jsx` rename duplicate `id="tv-back-btn-mylist"` at line 47 to `id="tv-back-btn-mylist-main"` and at line 68 to `id="tv-back-btn-mylist-empty"` to fix DOM duplicate id
- [ ] In `frontend/features/tv/screens/TvPlayerView.jsx` lines 18-19 replace `useState` for `showExitConfirm` with `tvStore.showExitConfirm` observable and `tvStore.setShowExitConfirm(bool)` action, keeping `useState` for `PlaybackComponent` (code-split switch)
- [ ] In `frontend/features/tv/screens/TvMyListView.jsx` lines 32-41 harden Escape keydown `useEffect` cleanup with `tvStore.isActive` guard inside the handler so listener no-ops when TV view is not active and remove listener on unmount
- [ ] In `frontend/components/remote/MasterScreen.jsx` lines 15-17 replace `useState` for `scanError` and `scanSuccess` with `remoteStore.scanError` and `remoteStore.scanSuccess` observables (keep `manualCode` as `useState` because it is local form state)
- [ ] In `frontend/components/remote/MasterScreen.jsx` lines 20-76 refactor `scannerContainerRef` from a `useCallback` ref callback that creates `Html5QrcodeScanner` into a `useEffect` with `[]` deps that runs once on mount and a cleanup that calls `scanner.clear()` on unmount to fix double-init race
- [ ] In `frontend/components/remote/MasterScreen.jsx` add `aria-label="QR code scanner"` to the scanner container `<div>` and wrap `scanError` message in `<div role="alert" aria-live="polite">` so screen readers announce scan failures
- [ ] In `frontend/components/remote/MasterScreen.jsx` manual code `<TextField>` add `onPaste` handler that uppercases pasted text and splits on whitespace to support pasting the full URL, plus `onBlur` that uppercases the field value before submit
- [ ] Add `frontend/e2e/tv-mode-boot.spec.js` Playwright test that opens `http://localhost:3000/?tv=1`, waits for `TvHomeView` to be visible (assert no crash), mocks the QR scanner, types a code, and asserts `SlaveConnectedView` shows
- [ ] In `frontend/components/remote/master/MasterRemotePlayerControlView.jsx` audit every `useState` and ensure none is used for transport state (playing, time, buffering) — those must come from `playbackStore` observables; only local form state (e.g. expansion toggles) may use `useState`
- [ ] In `frontend/components/remote/master/MasterRemoteProgressSlider.jsx` add `role="slider"` `aria-valuenow={currentTime}` `aria-valuemin={0}` `aria-valuemax={duration}` `aria-label="Playback progress"` and `onKeyDown` handler that seeks -10s on ArrowLeft and +10s on ArrowRight
- [ ] In `frontend/components/remote/master/MasterRemotePlayerControlView.jsx` add a floating "Chat with TV" button that opens a small `SecondScreenChat` panel sending `quix-tv-chat` messages through `websocketService` (new WS type, see backend-ws-server todo)
- [ ] Create `frontend/components/remote/master/SecondScreenChat.jsx` as a small chat panel with message list and input, scoped to current `remoteStore.activeSlaveId`, listening to `quix-tv-chat` via `wsRouter`
- [ ] Split `frontend/components/remote/master/EpisodesDrawer.jsx` (729L) into `HoloDrawerHeader.jsx`, `EpisodeRow.jsx`, `EpisodeActions.jsx`, `EmptyEpisodes.jsx` reusing the sub-components defined in `frontend/components/library/EpisodesDrawer.jsx` split (frontend-ui-views 5.2.2) to deduplicate the holoFieldSx recipe and the row markup
- [ ] In `frontend/components/remote/master/MasterRemotePlayerControlView.jsx` render floating emoji bubbles for 2s when `remoteStore.reactions` array receives a new entry, animated with `reducedMotion()` guard from `frontend/motion/grammar.js`
- [ ] In `frontend/views/playback/LocalPlaybackView.jsx` add a "Cast" floating action button (FAB) in the bottom-right that opens `MasterScreen` pairing flow, even when not in `?tv=1` mode — extracts `castItem(item)` action into `remoteStore`
- [ ] Add `frontend/e2e/master-cast-button.spec.js` Playwright test that opens LocalPlaybackView, clicks Cast FAB, asserts MasterScreen pairing modal opens, mocks WS handshake, asserts `remoteStore.isRemoteMaster` becomes true
- [ ] Add `frontend/e2e/tv-chat.spec.js` Playwright test that pairs master+slave, sends a chat message from MasterRemotePlayerControlView, asserts message appears on slave screen within 500ms
- [ ] In `frontend/components/remote/slave/SlavePairingView.jsx` wrap pulse animation CSS in `@media (prefers-reduced-motion: no-preference)` so users with `prefers-reduced-motion: reduce` see a static QR code without the pulsing ring
- [ ] In `frontend/components/remote/slave/SlavePairingView.jsx` add `aria-live="polite"` and `aria-atomic="true"` to the short-code display so screen readers announce the pairing code when it is generated or refreshed
- [ ] In `frontend/components/remote/slave/SlaveReconnectingView.jsx` add `role="status" aria-live="polite"` to the root container and expose the retry counter as `aria-label="Reconnection attempt {n} of 12"`
- [ ] In `frontend/components/remote/slave/SlaveConnectedView.jsx` add `aria-label="Disconnect from remote"` to the disconnect `<IconButton>` and verify focus ring is visible on `:focus-visible`
- [ ] In `frontend/components/media/SlaveVideoPlayer.jsx` when fullscreen is active, set `aria-modal="true"` on the player root and ensure the player container traps focus while in fullscreen
- [ ] In `frontend/components/remote/slave/SlaveScreen.jsx` and `SlavePairingView.jsx` implement focus trap: on mount auto-focus the short-code element (or copy button) and ensure Tab cycles within the pairing view
- [ ] In every file under `frontend/components/remote/slave/*.jsx` add a semantic `id` attribute on the root JSX element (e.g. `id="slave-pairing-view"`, `id="slave-reconnecting-view"`, `id="slave-connected-view"`)
- [ ] Add `frontend/e2e/slave-pairing.spec.js` Playwright test that opens `?tv=1`, mocks QR pairing from a master context, asserts the slave UI transitions from pairing to connected view with `aria-live` announcements
- [ ] Add `frontend/e2e/slave-reconnect.spec.js` Playwright test that opens slave, kills WS connection, asserts `SlaveReconnectingView` appears with retry counter visible, then restores WS and asserts recovery within 12 retries
- [ ] In `frontend/features/tv/screens/TvPlayerView.jsx` lines 52, 116, 138, 146, 152 replace hardcoded IT fallback strings ('Indietro', 'Esci', 'Caricamento...') with `t('tv.back')`, `t('tv.exit')`, `t('common.loading')` and remove the second-argument fallback so missing translations are visible
- [ ] Audit every file under `frontend/features/tv/` and replace any hardcoded user-facing string with `t('namespace.key')` from `frontend/hooks/useTranslations.js`, adding missing keys to `frontend/locales/en.js` and `frontend/locales/it.js`
- [ ] In `frontend/components/remote/MasterScreen.jsx` replace all hardcoded label strings (e.g. "Scan QR Code", "Enter code manually", "Connect") with `t('remote.scanQr')`, `t('remote.manualCode')`, `t('remote.connect')` and add the keys to both locale files
- [ ] Audit every file under `frontend/components/remote/` and replace any hardcoded user-facing string with `t()` calls, adding missing keys to `frontend/locales/en.js` and `frontend/locales/it.js`
- [ ] In `frontend/locales/en.js` and `frontend/locales/it.js` add any missing translation keys surfaced by the previous TV/remote audit tasks; ensure `Object.keys(en).length === Object.keys(it).length` and no key is empty
- [ ] Add `frontend/e2e/tv-i18n.spec.js` Playwright test that switches language EN→IT via `preferencesStore.setLanguage('it')`, asserts every text element under `frontend/features/tv/` matches an IT pattern (no leftover English strings), then switches back
- [ ] In `frontend/features/tv/screens/TvScreenRouter.jsx` wrap the async `import('./TvPlayerView')` and any other lazy import in `<Suspense fallback={<Skeleton id="tv-loading-suspense" />}>`
- [ ] In `frontend/features/tv/screens/TvPlayerView.jsx` add a `useEffect` global keydown handler that maps Space→togglePlay, F→toggleFullscreen, ArrowLeft/ArrowRight→seek ±10s, ArrowUp/ArrowDown→volume ±5, M→toggleMute, Escape→showExitConfirm, and cleans up on unmount
- [ ] In `frontend/features/tv/screens/TvMyListView.jsx` add keyboard navigation: ArrowUp/ArrowDown moves `focusedIndex` in the myList array, Enter calls `selectItem`, Backspace/Escape navigates back, with visible focus ring on the focused card
- [ ] In `frontend/features/tv/components/TvCard.jsx`, `TvListRow.jsx`, `TvQuickActionTile.jsx` apply a shared `className="tv-focusable"` and add a global CSS rule in `frontend/styles/tv-focus.css` (new file) that gives a 3px solid accent ring on `:focus-visible`
- [ ] Add `frontend/e2e/tv-keyboard-nav.spec.js` Playwright test that opens `?tv=1`, presses ArrowDown 3 times, asserts the third card has the `tv-focusable:focus-visible` ring, presses Enter, asserts detail view opens
- [ ] In `frontend/store/remoteStore.js` `triggerAutoFullscreen()` add a guard `if (/iPhone|iPad/.test(navigator.userAgent)) return` because iOS Safari does not support programmatic fullscreen from JS
- [ ] In `frontend/components/media/SlaveVideoPlayer.jsx` add a CSS-only fullscreen fallback using `:fullscreen` pseudo-class so the player fills the screen via CSS when the Fullscreen API is unavailable
- [ ] In `frontend/components/media/VideoPlayer.jsx` add a Picture-in-Picture button in the `HoloPlayerControls` toolbar that calls `videoRef.current.requestPictureInPicture()` and toggles to `exitPictureInPicture()` on second click; hide the button if `document.pictureInPictureEnabled` is false
- [ ] In `frontend/store/playbackStore.js` add a watcher that calls `videoEl.requestPictureInPicture()` automatically when `document.visibilityState === 'hidden'` AND `watchTogetherStore.isInRoom` is true, and exits PiP on `visibilitychange` back to visible
- [ ] Add `frontend/e2e/feature-pip.spec.js` Playwright test that opens a video, clicks the PiP button, asserts `document.pictureInPictureElement` is the video, clicks again, asserts PiP exits
- [ ] Add `frontend/e2e/slave-fullscreen.spec.js` Playwright test that starts playback on the slave and asserts `document.fullscreenElement` is set within 1s of `play` event
