# Todo
- [ ] In `Dockerfile` lines 5 and 31 change `FROM oven/bun:1` to `FROM oven/bun:1.2.5@sha256:<actual-digest>` (run `docker pull oven/bun:1.2.5 && docker inspect --format='{{index .RepoDigests 0}}' oven/bun:1.2.5` to obtain the digest); apply to both the builder and runtime stages; after the final `COPY` add `USER bun` (the official `oven/bun` image ships with a `bun` user at UID 999); verify with `docker run --rm oven/bun:1.2.5 id bun` returning `uid=999(bun)` — all in one sequential line
- [ ] Create `.dockerignore` at repo root containing: `.git`, `.gitignore`, `.env`, `.env.*`, `!/.env.example`, `node_modules`, `**/node_modules`, `bun.lockb`, `graphify-out`, `.playwright-mcp`, `test-results`, `playwright-report`, `*.log`, `Dockerfile*`, `docker-compose*`, `README.md`, `docs/`, `.vscode`, `.idea`
- [ ] In `docker-compose.yml` under the `app` service add `cap_drop: ['ALL']`, `security_opt: ['no-new-privileges:true']`, `restart: unless-stopped`, `mem_limit: 512m`, `cpus: '1.0'`, `read_only: true`, `tmpfs: ['/tmp:size=64m']`; add `env_file: - .env.production`; create `.env.production.example` at repo root with placeholders `JWT_SECRET=changeme`, `REDIS_URL=redis://redis:6379`, `FRONTEND_DEV_URL=http://localhost:3000`, `PROD_URL=https://streamflix.example.com`, `LOG_LEVEL=info`; remove the dead `IN_DOCKER=true` env from the `app` service environment block (verified by `grep -rn IN_DOCKER backend/src frontend/src 2>/dev/null` returning no results); add `logging: { driver: json-file, options: { 'max-size': '10m', 'max-file': '3' } }` to prevent log files from filling the disk — all in one sequential line
- [ ] Create `Caddyfile` at repo root with: `:443 { tls internal; reverse_proxy app:3000 { health_uri /health health_interval 10s health_timeout 5s } encode zstd gzip } :80 { redir https://{host}{uri} permanent }`; also add a rate-limit block if Caddy 2.7+ with `order rate_limit before reverse_proxy`
- [ ] In `docker-compose.yml` add a `caddy` service: `image: caddy:2-alpine`, `ports: ['80:80', '443:443']`, `volumes: ['./Caddyfile:/etc/caddy/Caddyfile:ro', 'caddy-data:/data', 'caddy-config:/config']`, `restart: unless-stopped`, `depends_on: { app: { condition: service_healthy } }`; add `caddy-data` and `caddy-config` to the top-level `volumes:`; add a `redis` service: `image: redis:7-alpine`, `restart: unless-stopped`, `volumes: ['redis-data:/data']`, `healthcheck: { test: ['CMD', 'redis-cli', 'ping'], interval: 5s }`; add `redis-data` to the top-level `volumes:` — all in one sequential line
- [ ] In `frontend/eslint.config.js` verify the `no-restricted-imports` rule already blocks direct store imports from views; if missing, add `{ paths: [{ name: 'frontend/store/mediaStore', message: 'Use useStores() instead' }, ...], patterns: ['frontend/store/*'] }`; add a custom rule (or use `eslint-plugin-jsx-a11y` with extension) that requires an `id` prop on the root element of every exported component; report as `error` with message "Component must have id on root element per AGENTS.md"; add `{ rules: { 'max-lines': ['error', { max: 800, skipComments: true, skipBlankLines: true }] } }` with a `// @allow-large-file` exception comment parser; add a custom rule that errors on `useState` calls in files matching `frontend/views/**/*.jsx`, `frontend/features/**/*.jsx`, `frontend/components/*/[A-Z]*.jsx` (screen-level files); allow only in form-state comments — all in one sequential line
- [ ] Verify the JetBrains MCP format tool is wired to the project; if not, add a `.editorconfig` at repo root with `root = true`, `[*] indent_style = space`, `indent_size = 2`, `end_of_line = lf`, `charset = utf-8`, `trim_trailing_whitespace = true`, `insert_final_newline = true`
- [ ] In `package.json` add `"lint:fix": "eslint . --fix"` script; add `"verify": "bash scripts/verify.sh"`; add `"commit": "cz"` and install `commitizen` + `cz-conventional-changelog` as devDependencies; add `"config": { "commitizen": { "path": "./node_modules/cz-conventional-changelog" } }`; add `"graphify:update": "bash scripts/graphify-update.sh"` and `"graphify:report": "bunx graphify export callflow-html"` scripts — all in one sequential line
- [ ] Create `scripts/verify.sh` (bash) that runs in order: `set -e` → `docker build -t stream-flix .` → `docker compose down` → `docker compose up -d --build` → `sleep 15` → `docker logs --tail 200 app 2>&1 | grep -E "(ERROR|FATAL)" && exit 1 || true` → `bunx playwright test frontend/e2e/smoke.spec.js` → `bunx graphify export callflow-html`; exits 0 only if all pass
- [ ] Create `.husky/pre-commit` (bash) that runs `bun run lint && bunx vitest run --reporter=dot` (the fast subset of verify, not the full Docker cycle)
- [ ] Create `.gitmessage` at repo root with the conventional commit template: `feat|fix|docs|style|refactor|test|chore|perf|ci(scope): subject\n\n# Why:\n# What:\n# Test:\n# Tokens used:\n# Time: HH:MM`
- [ ] Create `.husky/post-commit` (bash) that runs `find graphify-out/GRAPH_REPORT.md -mtime +7` and warns (does not fail) if the graph is older than 7 days
- [ ] Create `.github/workflows/verify.yml` that on `pull_request` runs `bun run verify` on a self-hosted runner labeled `docker`, posts the result back to the PR as a check, and uploads `.playwright-mcp/` + `test-results/` artifacts; add a step `bun run graphify:update` after lint and before e2e, so the graph is refreshed on every PR
- [ ] Create `scripts/graphify-update.sh` (bash) that runs `bunx graphify export callflow-html && bunx graphify update . && git add graphify-out/` (does not commit; user reviews the diff)
- [ ] Verify `.graphify.env` exists and contains `GRAPHIFY_PROJECT=stream-flix`, `GRAPHIFY_OUTPUT=graphify-out`, `GRAPHIFY_FORMAT=json,html`; if missing, create it
- [ ] In `frontend/vite.config.js` add `build.rollupOptions.output.manualChunks` that splits `mediaStore`, `remoteStore`, `watchTogetherStore`, `playbackStore`, `libraryStore`, `searchStore` into separate chunks; also split `mui` and `gsap` into vendor chunks; add a `vite-plugin-critical` (or manual `<style>` injection in `frontend/index.html` via a build step) for the above-the-fold CSS to reduce render-blocking
- [ ] In `frontend/index.html` add `<link rel="preload" as="font" type="font/woff2" crossorigin>` for each custom font used (check `frontend/styles/fonts.css`), and add `<link rel="preconnect" href="https://image.tmdb.org">` for poster images; add `<link rel="manifest" href="/manifest.webmanifest">` and `<meta name="theme-color" content="#1a1a1a">` in the `<head>`
- [ ] Run `find frontend/src -name "*.jpg" -o -name "*.png"` and convert large images to AVIF + WebP fallback, add `loading="lazy"` and `decoding="async"` to every `<img>` and MUI `<CardMedia>`
- [ ] In `frontend/public/sw.js` (created by library-and-integrations 7.5.3) ensure the cache strategies are: app shell `CacheFirst`, TMDB posters `StaleWhileRevalidate`, API calls `NetworkOnly`; the perf budget test asserts no main-thread block > 50ms; add `self.addEventListener('message', (e) => { if (e.data.type === 'SKIP_WAITING') self.skipWaiting() })` and a frontend listener that detects `sw.updateFound` and shows a snackbar "New version available, reload?"
- [ ] Create `.github/workflows/ci.yml` with jobs: `lint` (runs `bunx eslint .`), `typecheck` (runs `bunx tsc --noEmit` if TS is added later; otherwise skip), `unit` (runs `bunx vitest run`), `build` (runs `bun run build` and uploads `frontend/dist/`)
- [ ] Create `.github/workflows/e2e.yml` that on push/PR runs `docker compose up -d --build`, waits for `/health`, runs `bunx playwright test frontend/e2e/ --reporter=html`, uploads `playwright-report/` and `.playwright-mcp/` artifacts
- [ ] Create `.github/workflows/release.yml` that on `push tags: v*` runs `bunx semantic-release` (or manual version bump + `docker build -t stream-flix:$TAG . && docker push`), generates a CHANGELOG.md entry, and creates a GitHub release
- [ ] In GitHub repo settings (documented in `.github/BRANCH_PROTECTION.md`) require status checks: `lint`, `e2e`, `verify`; require 1 review; disallow direct push to `main`
- [ ] In `backend/src/index.js` replace every `console.log/error/warn` call with `logger.info/error/warn` imported from `backend/src/logger.js`
- [ ] Create `backend/src/logger.js` exporting a configured `pino` instance: `pino({ level: process.env.LOG_LEVEL || 'info', base: { service: 'stream-flix-backend', env: process.env.NODE_ENV }, timestamp: pino.stdTimeFunctions.isoTime })`
- [ ] In `frontend/services/websocketService.js` add a `LOG_LEVEL` env read in the constructor and wrap all `console.log/error/warn` calls in `if (LOG_LEVEL === 'debug')`; expose `setLogLevel(level)` for dev