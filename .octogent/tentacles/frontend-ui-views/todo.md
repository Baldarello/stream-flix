# Todo
- [ ] In `frontend/views/features/FeatureRouter.jsx` lines 104-107 remove the invalid `sx` prop from `<main>` and apply the styles to an inner `<Box component="main" id="feature-main" sx={...}>` so the styling actually applies; add a visually hidden skip-link `<a href="#feature-main" id="skip-link">Skip to main content</a>` that becomes visible on `:focus-visible`
- [ ] In `frontend/index.jsx` lines 10-12 replace the once-evaluated `shouldUseTvMode` const with a `useMediaQuery(theme.breakpoints.down('sm'))` hook that re-evaluates on resize, and pass it as a prop into `ViewSwitch`; lines 14-27 in `LoadingFallback`, wrap the spinner in a `useReducedMotion()` check that returns a static text instead of the animated spinner, and add `id="app-loading"` to the root; line 25 replace the hardcoded `Caricamento...` with `t('common.loading')`
- [ ] In `frontend/views/ViewSwitch.jsx` lines 94-134 wrap each view branch (`HomeView`, `SearchView`, `LibraryView`, etc.) in `<Suspense fallback={<Skeleton id="view-loading-suspense" />}>`; lines 46-50 in the `wrap()` helper, add `id={\`screen-\${viewKey.toLowerCase()}\`}` to the wrapper so every view has a unique screen id; add a route `'Preferences'` that renders `<PreferencesView />` inside the standard `<Suspense>` wrapper
- [ ] In `frontend/views/features/SearchView.jsx` extract a new `SearchShell` component that takes `{ header, children, height }` and renders the `<Header />` + container with `minHeight: 'calc(100vh - 64px - 200px)'` + `pt: 'calc(64px + env(safe-area-inset-top))'`; refactor the 3 branches (prompt, loading, results) to use it; add `id="screen-search-prompt"`, `id="screen-search-loading"`, `id="screen-search-results"` to the 3 branch roots; add `<div role="status" aria-live="polite" aria-atomic="true">{t('search.resultsCount', { count })}</div>` above the result grid
- [ ] In `frontend/components/feedback/NotificationSnackbar.jsx` add `role="status"` and `aria-live="polite"` to the root `<Snackbar>` (or its inner `<Alert>`) so screen readers announce notifications; ensure the Snackbar has `id="notification-snackbar"` and that each `<Alert>` inside has a unique `id` based on its message key
- [ ] In `frontend/components/modals/ModalShell.jsx` add `aria-modal="true"` to the root dialog, add `aria-labelledby={titleId}` where `titleId` is the id of the title `<Typography>` or `<h2>`, and localize the close `aria-label` via `t('common.close')`; line 162 replace the hardcoded `aria-label="close"` with `aria-label={t('common.close')}`
- [ ] In `frontend/components/feedback/Skeleton.jsx` and `SkeletonCard.jsx` add an `id` prop defaulting to `'skeleton-default'` that is applied to the root element
- [ ] In `frontend/components/media/DetailView.jsx` (1004L) extract `DetailHero.jsx` containing the backdrop image, poster, title, rating, genres, and release info; accept `{ item }` and render only the hero section; extract `DetailActions.jsx` containing the Play, Add to List, Link Video, and Share buttons; accept `{ item, onPlay, onAddToList, onLinkVideo }` props; extract `DetailEpisodes.jsx` containing the season picker and episode grid; accept `{ item, onPlayEpisode }` props; extract `DetailDialogs.jsx` containing the edit modal, create modal, link-edit modal; accept `{ openDialog, onClose, item }` props; after the 4 extractions, rewrite the parent to be a <300L composer that orchestrates the sub-components and manages dialog state via `uiStore`
- [ ] In `frontend/components/library/EpisodesDrawer.jsx` (933L) extract `HoloDrawerHeader.jsx` containing the drawer header, close button, and season selector; extract `EpisodeRow.jsx` containing a single episode row with thumbnail, title, duration, and play button; extract `EpisodeActions.jsx` containing the play, link, edit, remove buttons for an episode; extract `EmptyEpisodes.jsx` containing the empty state with a CTA to link the first episode; after the 4 extractions, rewrite the parent to be a <300L composer using the sub-components; add `<EpisodeProgressRing progress={watchedPercent} size={32} />` next to the duration chip and a 2px progress bar at the bottom of the thumbnail — all in one sequential line
- [ ] In `frontend/components/media/CinematicHero.jsx` (305L) extract `HeroBackdrop.jsx` containing the backdrop image, gradient overlay, and mouse-move parallax effect; extract `HeroTitle.jsx` containing the title, tagline, rating, and meta info; extract `HeroActions.jsx` containing the Play and More Info buttons; after the 3 extractions, rewrite the parent to be a <200L composer using the sub-components; lines 116-127 in the mouse-move parallax handler, wrap the listener registration in `if (!window.matchMedia('(prefers-reduced-motion: reduce)').matches)` so no parallax is applied for users with reduced motion preference
- [ ] In `frontend/components/media/CinematicDetail.jsx` add 3 tabs: "Reviews" (load from `apiCall.getReviews(id)`), "Where to Watch" (load from `apiCall.getWatchProviders(id)`), "More Like This" (load from `apiCall.getRecommendations(id)`); use MUI `<Tabs>` and `<TabPanel>` with `id="detail-tab-reviews"` etc.
- [ ] Create `frontend/components/media/EpisodeProgressRing.jsx` as a primitive that accepts `{ progress, size, stroke }` and renders a circular SVG progress indicator (0-100%)
- [ ] In `frontend/components/overlay/OverlayLayer.jsx` lines 34-63 wrap each modal in `React.lazy(() => import('../modals/...Modal.jsx'))` and render with `<Suspense fallback={<Skeleton />}>`; move the inline `ShareLibraryModal`, `LinkMovieModal`, etc. mounts into `frontend/components/overlay/OverlayLayer.jsx` so they share the same lazy-load and focus-trap behavior
- [ ] Run `grep -rEn "['\"]([A-Z][a-z]+ )+[a-z]+['\"]" frontend/components/` and replace every match in JSX text nodes with `t()` calls; add missing keys to `frontend/locales/en.js` and `it.js`
- [ ] Run `grep -rEn "['\"]([A-Z][a-z]+ )+[a-z]+['\"]" frontend/views/ frontend/features/home/` and replace every match in JSX text nodes with `t()` calls
- [ ] In `frontend/locales/en.js` and `frontend/locales/it.js` run `node -e "const a=require('./frontend/locales/en.js'); const b=require('./frontend/locales/it.js'); console.log(Object.keys(a).filter(k=>!b[k]||!a[k]))"` to find missing keys, add them to both files
- [ ] Run `grep -rn "t([^,)]*, *['\"]" frontend/` and remove every second-argument fallback in `t()` calls; missing keys must be visible during dev, not hidden
- [ ] Create `frontend/utils/componentId.js` exporting `componentId(prefix)` that returns a stable id like `${prefix}-${useId()}` memoized per component instance
- [ ] In `frontend/App.jsx` lines 161-178 add `id="app-root"` to the root `<ThemeProvider>` (or its inner wrapper)
- [ ] In `frontend/features/home/HomeView.jsx` line 39 replace the bare `<>` fragment with `<Box id="screen-home" ...>` so the home screen has a unique id; at the top, render `<ResumeHero item={mediaStore.lastWatched} />` if `mediaStore.lastWatched` is not null, otherwise render the normal `CinematicHero`
- [ ] Run `grep -L "id=" frontend/components/**/*.jsx frontend/features/home/*.jsx frontend/views/**/*.jsx` and add an `id` attribute to every file that does not have one on its root JSX element
- [ ] In `frontend/eslint.config.js` add a custom rule `jsx-a11y/id-must-exist-on-component-root` (or use `react/jsx-no-undef` + a custom plugin) that errors when a component file's default export has no `id` on its outermost element
- [ ] In `frontend/components/media/VideoPlayer.jsx` add `role="slider" aria-valuenow={currentTime} aria-valuemin={0} aria-valuemax={duration} aria-label={t('player.progress')}` to the progress bar, and add `<track kind="captions" srcLang="en" label="English" />` (empty default) inside the `<video>` element; add a Picture-in-Picture button that calls `videoRef.current.requestPictureInPicture()` (and `exitPictureInPicture()` on second click); hidden if `'pictureInPictureEnabled' in document && !document.pictureInPictureEnabled`; add a Sleep Timer popover button next to the volume slider with options [15, 30, 60, "End of episode"] that calls `playbackStore.setSleepTimer(min)` and displays `sleepTimerRemainingSec` as a countdown chip; add a Captions/Subtitles button that opens a popover with the available `<track>` elements and a "Custom URL" field that loads a `.vtt` file; persists the choice in `preferencesStore`; add an Audio Language dropdown that lists the `mediaLinks[].language` options and switches the active `<audio>` element; add a new `UpNextTray` that mounts 30s before the video's `ended` event, showing the next item from `playbackStore.upNext` with a countdown and click-to-jump; in the `HoloPlayerControls`, add an A-B loop button: click "Set A" stores `currentTime` as loopStart, click "Set B" stores `currentTime` as loopEnd and sets `video.loop = true` plus a timeupdate listener that resets to loopStart when currentTime >= loopEnd; add a frame-step button: pressing `,` (comma) does `videoRef.current.currentTime -= 1/30`, pressing `.` (dot) does `currentTime += 1/30`; in the `currentVideoSrc` useEffect (around line 539), check `if (!navigator.onLine && downloadedBlobs[item.id])` and set the video `src` to `URL.createObjectURL(downloadedBlobs[item.id].blob)` as a fallback; in the `currentVideoSrc` useEffect, add the check `if (!navigator.onLine) { const dl = await db.downloadedBlobs.get(item.id); if (dl) return URL.createObjectURL(dl.blob) }` before the regular `video_url` fetch; add a `ReactionsLayer` overlay that listens to `remoteStore.reactions` and renders each emoji as a floating bubble for 2s, with a CSS animation that respects `prefers-reduced-motion`; add `useSwipeGestures(containerRef, { onSwipeRight: () => seekForward(10), onSwipeLeft: () => seekBackward(10), onSwipeUp: () => volumeUp(), onSwipeDown: () => volumeDown() })` for mobile players — all in one sequential line
- [ ] In `frontend/components/layout/CinematicRow.jsx` add `aria-roledescription="carousel"` to the row root, `aria-label={t('row.label', { title })}` to the section, and an `onKeyDown` handler that scrolls left/right by one card width on ArrowLeft/ArrowRight when the row has focus; add `tabIndex={0}` and an `onKeyDown` that scrolls the inner container by one card width on ArrowLeft/ArrowRight; keep the scroll-flag `useState` for `canScrollLeft/canScrollRight` (visual only), but move the drag state (dragItemId/dropTargetId) to `dragStateStore` via `useDragState()`; lines 49-52 replace drag `useState` with `useDragState()`; keep the scroll-flag `useState` for `canScrollLeft/canScrollRight` because it is purely visual
- [ ] In `frontend/components/library/MyListDetailView.jsx` lines 114-118 replace the 5 `useState` calls (`filter`, `sort`, `pendingRemove`, `dragItemId`, `dropTargetId`) with `libraryStore.filter`, `libraryStore.sort`, `libraryStore.pendingRemove`, and the new `useDragState` hook; lines 84-94 replace the duplicated `formatDate` and `matchesQuery` helpers with imports from `frontend/utils/dateFormat.js` and `frontend/utils/matchesQuery.js` (created in the dedup task); add a dropdown to switch the active list and a drag handle to drag items between lists
- [ ] In `frontend/components/library/EpisodesDrawer.jsx` replace the local drag state (dragItemId/dropTargetId) with `useDragState()`; remove the corresponding `useState` calls and the local handler
- [ ] In `frontend/components/library/VideoLinksTab.jsx` line 57 replace `useState` for `expandedShowId` with `libraryStore.expandedShowId` (add the observable) and persist it on toggle
- [ ] Create `frontend/components/layout/ResumeHero.jsx` as a variant of `CinematicHero` that shows the last-watched item with a progress ring overlay and a "Resume" button; falls back to a static poster if no progress
- [ ] In `frontend/components/library/MyListTab.jsx` and `ContinueWatchingTab.jsx` wrap the remove confirmation dialog in `<div role="alert" aria-live="assertive">` so screen readers announce the confirmation prompt; lines 193-212 and `ContinueWatchingTab.jsx` lines 222-241 extract the inline confirm-remove pattern into a shared `ConfirmRemoveDialog` component in `frontend/components/library/shared/ConfirmRemoveDialog.jsx`; line 63 replace the local `useState` for `pendingRemove` with `libraryStore.pendingRemove` (and a `libraryStore.setPendingRemove(item)` action)
- [ ] In `frontend/components/layout/CinematicGrid.jsx` lines 92-115 replace the hardcoded 8-skeleton placeholder with a check `if (loading) { return <SkeletonGrid count={8} /> }` that reads `mediaStore.homePageRowsLoading` and avoids the empty-state flash when data is in-flight
- [ ] In `frontend/components/media/HoloPlayerControls.jsx` add a Picture-in-Picture button that calls `videoRef.current.requestPictureInPicture()` and toggles to `exitPictureInPicture()` on second click; hide if `document.pictureInPictureEnabled` is false
- [ ] In `frontend/components/library/LibraryDashboard.jsx` add a "Profile" chip in the header that shows the active profile avatar and name, opens `ProfileSwitcherModal` on click; add a long-press (mobile) / Shift-click (desktop) handler that enters selection mode and shows a bulk-action bar (Move to list, Remove, Tag) at the bottom; add a horizontal chips row above the manual lists showing the 4 smart collections (`unwatchedMovies`, `unfinishedSeries`, `addedThisMonth`, `trendingInMyList`); clicking a chip filters the grid below; on each card add a small "downloaded" badge (cloud-check icon) if `db.downloadedBlobs.get(item.id)` returns a row, plus a download icon button that triggers `downloadStore.downloadItem(item.id, item.video_url)`; add a download progress UI: a thin bar at the bottom of the card that shows the `ReadableStream` progress from the `/api/fetch-blob` response; render a chips row above the manual lists: for each rule, a chip with the rule name and item count; clicking filters the grid to that rule's items — all in one sequential line
- [ ] In `frontend/components/modals/LinkEpisodesModal.jsx` (599L) if the file exceeds 800L after the parent task list, split it into `LinkEpisodesForm.jsx`, `LinkEpisodesPreview.jsx`, `LinkEpisodesActions.jsx`
- [ ] In `frontend/components/modals/MediaSyncModal.jsx` (412L) split it into `MediaSyncConfirm.jsx`, `MediaSyncProgress.jsx`, `MediaSyncError.jsx` if it exceeds 800L after this task
- [ ] In `frontend/components/modals/EpisodeInfoModal.jsx` verify it has `id="episode-info-modal"`, focus is trapped inside while open, and Escape closes it
- [ ] In `frontend/components/feedback/NotificationsModal.jsx` verify it has `id="notifications-modal"`, an `aria-live` region for new notifications, and that the list is keyboard-navigable; listen to the new `backup-completed` event from `syncStore` and show a success toast with a "View in Drive" link
- [ ] In `frontend/components/modals/ShareLibraryModal.jsx` extend the form to allow selecting a specific named list (from `libraryStore.myLists`) to share instead of the entire library
- [ ] Run `grep -L "import EmptyState" frontend/components/media/ErrorView.jsx frontend/components/remote/master/EpisodesDrawer.jsx frontend/features/tv/screens/*.jsx` and replace any custom empty-state markup with `<EmptyState title={...} action={...} id="..." />` from `frontend/components/library/shared/EmptyState.jsx`
- [ ] In `frontend/hooks/useTranslations.js` and `frontend/components/layout/Header.jsx` add a `useEffect` that calls `document.documentElement.lang = preferencesStore.language` whenever it changes
- [ ] In `frontend/styles/themePalettes.js` add a `highContrastPalette` object with WCAG AAA-contrast colors, and in the theme provider switch to it when `window.matchMedia('(prefers-contrast: more)').matches` is true; add a `highContrastPalette` object that uses `#FFFFFF` on `#000000` for text and `#FFFF00` for accents; switch the active palette when `window.matchMedia('(prefers-contrast: more)').matches`
- [ ] In `frontend/services/apiCall.jsx` add `getReviews(id, type)` that calls TMDB `/movie/{id}/reviews` or `/tv/{id}/reviews` based on `type`, returns `{ id, author, content, rating, createdAt }[]`, with a 24h cache key; add `getWatchProviders(id, type)` that calls TMDB `/movie/{id}/watch/providers` or `/tv/{id}/watch/providers`, returns `{ country, flatrate?, rent?, buy? }[]` keyed by ISO country code; add `getRecommendations(id, type)` that calls TMDB `/movie/{id}/recommendations` or `/tv/{id}/recommendations`, returns TMDB `results` array (limited to 20), with a 24h cache; add `searchSuggest(prefix)` that calls TMDB `/search/multi?query=${prefix}&include_adult=false`, returns the first 5 results with `{ id, mediaType, title, posterPath }`; add `getTrendingByGenre(genreId, type='movie')` that calls TMDB `/discover/${type}?with_genres=${genreId}&sort_by=popularity.desc`, returns the first 20 results; add `getSeasonRecap(seasonId)` that fetches `/tv/{seriesId}/season/{seasonNumber}` from TMDB and returns `{ name, overview, episodeCount, airDate }`; refactor the fetch helpers to support a `retries: 3, backoffMs: 500` option that wraps the fetch in an exponential backoff loop (500ms, 1000ms, 2000ms) and only retries on 5xx and network errors — all in one sequential line
- [ ] In `frontend/components/media/VideoPlayer.jsx` in the `HoloPlayerControls`, add a Picture-in-Picture button that calls `videoRef.current.requestPictureInPicture()` (and `exitPictureInPicture()` on second click); hidden if `'pictureInPictureEnabled' in document && !document.pictureInPictureEnabled`; add a Sleep Timer popover button next to the volume slider with options [15, 30, 60, "End of episode"] that calls `playbackStore.setSleepTimer(min)` and displays `sleepTimerRemainingSec` as a countdown chip; add a Captions/Subtitles button that opens a popover with the available `<track>` elements and a "Custom URL" field that loads a `.vtt` file; persists the choice in `preferencesStore`; add an Audio Language dropdown that lists the `mediaLinks[].language` options and switches the active `<audio>` element (or requests a new video URL for the chosen language); add a new `UpNextTray` that mounts 30s before the video's `ended` event, showing the next item from `playbackStore.upNext` with a countdown and click-to-jump; add a `ReactionsLayer` overlay that listens to `remoteStore.reactions` and renders each emoji as a floating bubble for 2s, with a CSS animation that respects `prefers-reduced-motion`; in the `currentVideoSrc` useEffect, add `if (!navigator.onLine) { const dl = await db.downloadedBlobs.get(item.id); if (dl) return URL.createObjectURL(dl.blob) }` before the regular `video_url` fetch — all in one sequential line
- [ ] Create `frontend/hooks/useSwipeGestures.js` exporting `useSwipeGestures(ref, { onSwipeLeft, onSwipeRight, onSwipeUp, onSwipeDown, threshold = 50 })` that listens to `touchstart/touchmove/touchend` and dispatches the right callback based on delta
- [ ] Create `frontend/components/library/MoodLobby.jsx` as a grid of mood cards ("Cozy Night", "Horror Marathon", "Feel-Good", "Mind-Bending") each linking to a filtered grid of items from `catalogStore.itemsByMood(moodId)` which calls `apiCall.getTrendingByGenre(genreId)`
- [ ] Create `frontend/components/modals/RecapModal.jsx` that triggers when opening a series episode where `episode.season > 1 && episode.number === 1`; renders a 30-sec auto-scrolling text summary built from `apiCall.getSeasonRecap(previousSeasonId)` and `cachedItems[item.id].overview`
- [ ] In `frontend/components/modals/WatchTogetherModal.jsx` add a "Vote for next" section that shows the top 3 candidates (from `upNext`) and lets each participant tap a heart; tally is synced via `quix-vote` and the winner is auto-enqueued in `upNext`; refactor the 412L file into `WatchTogetherRoom.jsx` (room list, participants), `WatchTogetherChat.jsx` (chat + reactions), `WatchTogetherControls.jsx` (host controls, voting) if the file exceeds 600L after this task