# Todo
- [ ] Create `backend/src/ssrfGuard.js` exporting `safeFetch(url, opts)` that uses `dns.lookup` to resolve the hostname, blocks RFC1918 (10/8, 172.16/12, 192.168/16), 127.0.0.0/8, 169.254.0.0/16, ::1, fc00::/7, throws on private IP, and follows redirects up to 3 hops re-validating each hop
- [ ] Create `backend/src/auth.js` exporting `signToken({ sub, scope, ttlSeconds })` and `verifyToken(token)` using Node's `crypto.createHmac('sha256', SECRET)` with HS256, base64url encoding, and `SECRET` from `process.env.JWT_SECRET` (fail fast on missing env)
- [ ] Create `backend/src/validators.js` exporting `validateChatMessage`, `validateRegisterSlave`, `validateRegisterMaster`, `validateRemoteCommand`, `validateCreateRoom`, `validateJoinRoom`, `validateSelectMedia`, `validateMediaItem` — each is a pure function returning `{ ok, errors }` and never throws
- [ ] Create `backend/src/rateLimit.js` exporting `createLimiter({ capacity, refillPerSecond })` returning a token-bucket function `tryConsume(key)` that uses an in-memory `Map<key, { tokens, lastRefill }>`; add `cleanupIdle(now)` that removes entries idle > 1h
- [ ] In `backend/src/index.js` lines 65-94 replace direct `fetch(url)` inside `POST /api/validate-link` with `await safeFetch(url, { timeout: 5000 })` and return 403 on `SsrfBlockedError`; add `POST /api/fetch-blob` (mounted before `/api/validate-link`) that accepts `{ url }`, calls `safeFetch(url, { timeout: 30000 })`, streams the response body as `application/octet-stream` with `Content-Disposition: attachment`; add `POST /api/session` that accepts `{ googleIdToken }`, verifies via `google-auth-library`, looks up or creates user, calls `signToken({ sub: userId, scope: 'ws:write' })`, returns `{ token, expiresAt }`; wrap every route under `/api/*` (except `/health`, `/api`, `/api/session`) with `requireAuth` middleware reading `Authorization: Bearer <token>`, calling `verifyToken`, attaching `req.user = { sub }`, returning 401 on failure; lines 41-46 fix CORS: replace `origin: '*'` with `origin: [process.env.FRONTEND_DEV_URL, process.env.PROD_URL].filter(Boolean)`, remove `credentials: true`, keep `allowedHeaders: ['Content-Type', 'Authorization']`; lines 206-219 fix heartbeat loop: every 30s call `ws.ping()` on each client, on `ws.on('pong', ...)` flip `ws.isAlive = true`, if after one full interval `ws.isAlive` still false call `ws.terminate()` and remove from `wsServer.clients`; add per-IP rate limits: `/api/validate-link` 10 req/min, `/api/fetch-blob` 5 req/min, returning 429 with `Retry-After` header; replace every `console.log/error/warn` with `logger.info/error/warn` imported from `backend/src/logger.js`; create `backend/src/logger.js` exporting configured pino instance with `level: process.env.LOG_LEVEL || 'info'`, `base: { service: 'stream-flix-backend' }`, `timestamp: pino.stdTimeFunctions.isoTime` — all in one sequential line
- [ ] In `backend/src/index.js` around line 96 modify the `ws('/ws', { ... })` registration to read `headers.authorization`, call `verifyToken`, on failure send close code 4401 with reason "Unauthorized"; pass the verified `sub` into `getWSData` for downstream handlers
- [ ] In `backend/src/wss.js` around line 360 in the `quix-chat-message` handler, after parsing, call `validateChatMessage(payload)`; reject with `quix-error` if not ok; cap `text` at 2048 bytes and base64 `image` at 262144 bytes (256KB) with a clear error; around lines 288-337 in `quix-create-room` and `quix-join-room` handlers, call `validateCreateRoom` / `validateJoinRoom`; cap `username` at 32 chars, `roomId` at 8 chars, enforce `room.participants.size < 10` before adding; around line 430 in `quix-register-slave` handler, call `validateRegisterSlave`; require `slaveId` to match `/^[a-zA-Z0-9_-]{4,64}$/`; reject on any other shape; around line 559 in `quix-register-master` handler, call `validateRegisterMaster`; apply same `slaveId` regex; reject if `slaveId` does not exist in `remoteSessions` (slave offline); around line 671 in `quix-remote-command` handler, call `validateRemoteCommand`; restrict `command` to enum `['play', 'pause', 'stop', 'seek_to', 'seek_forward', 'seek_backward', 'play_item', 'select_item', 'clear_selection', 'skip_intro', 'request_status', 'request-media-sync']`; if `item` present call `validateMediaItem`; around line 269 in the main `onMessage` handler, add explicit branches for `Buffer.isBuffer(message)` (decode UTF-8), `message instanceof ArrayBuffer` (decode UTF-8), and `message.arrayBuffer` (Blob) before the existing string branch; replace every call to `broadcastRoomState(roomId)` with targeted messages: `sendChatMessage(roomId, msg)` for new chat, `sendParticipantJoined(roomId, user)` for joins, `sendPlaybackUpdate(roomId, state)` for playback, `sendMediaUpdate(roomId, media)` for media change; never rebroadcast the full state; line 16 remove the unused `mediaSyncProgress` Map declaration and any code that writes to it; lines 476-516 in the slave re-register handler, replace synchronous `oldWs.close()` with `queueMicrotask(() => oldWs.close(1000, 'replaced'))` so the old socket's `handleDisconnectQuix` runs after the new session is set in the `remoteSessions` Map; introduce a `sessionGeneration` counter per `slaveId`; increment on every successful register-slave; in `handleDisconnectQuix` (line 142) check the generation before destroying the session — if it was incremented, no-op; around line 613 wrap the `register-master` body in `async` mutex: `await masterMutex.runExclusive(async () => { ... })` using a tiny in-file async mutex (Map<slaveId, Promise>) to serialize concurrent master registers for the same slave; around line 296 change `generateId('room')` to a retry loop: call `generateId('room')`, check `rooms.has(id)`, if collision retry up to 5 times then throw; at the top of the message router, add a rate-limit chain: per `sub` sliding 100 msg/s, chat type 5/s, `quix-register-slave` 1 per 30s, `quix-register-master` 1 per 5s; on limit, send `{ type: 'quix-rate-limited', retryAfterMs }` and drop the message — all in one sequential line
- [ ] In `backend/src/wss.js` add new message type handlers: `quix-tv-chat` receives `{ slaveId, text }` from master, looks up `slaveWs` in `remoteSessions`, forwards as `{ type: 'quix-tv-chat', text, from: 'master', ts: Date.now() }`; `quix-reaction` receives `{ slaveId, emoji }` from any room participant, broadcasts to all room members AND forwards to slave if master→slave context active; `quix-vote` receives `{ roomId, itemId }`, increments Redis counter `room:{id}:vote:{itemId}` with TTL 5 minutes, broadcasts `quix-vote-tally` with current counts every 2s while voting open; `quix-group-control` receives `{ groupId, action, slaveIds[] }`, stores group membership in Redis `group:{groupId}`, `action: 'broadcast'` forwards embedded command to all `slaveIds` in the group; ensure every new message type is added to the rate limiter and validated via functions in `backend/src/validators.js` — all in one sequential line
- [ ] Add `@redis/client` to `backend/package.json` dependencies and run `bun install`
- [ ] Create `backend/src/redisStore.js` exporting `getClient()` (singleton with reconnect) and helpers `get/set/del/incr/expire/hSet/hGetAll/lPush/lRange/sAdd/sMembers` with automatic JSON serialization; read `REDIS_URL` from env
- [ ] In `backend/src/wss.js` line 10-20 refactor all 7 module-level Maps (`rooms`, `remoteSessions`, `shortCodeToSlaveId`, `slaveIdToShortCode`) to read/write through `redisStore` with key prefixes `room:{id}`, `session:{slaveId}`, `code:{shortCode}`, `slaveCode:{slaveId}`, all with TTL 3600s refreshed on every write; migrate `intentionallyDisconnectingSlaves` and `intentionallyDisconnectingMasters` from `Set` to Redis keys with TTL 60s using `SET key 1 EX 60`; add LRU cap on `slaveIdToShortCode` to 10000 entries: on every write, if `redisStore.incr('slaveCode:count') > 10000` then evict oldest entry via `ZADD slaveCode:lru score=now member=slaveId` + `ZPOPMIN score` pair; replace `broadcastRoomState` (line 78-96) with Redis pub/sub: each backend instance subscribes to `room:{id}:events`, room-state changes are `PUBLISH`'d, every instance forwards to its locally connected clients — all in one sequential line
- [ ] In `backend/src/docker-compose.yml` add a `redis` service: `image: redis:7-alpine`, `restart: unless-stopped`, `volumes: ['redis-data:/data']`, `healthcheck: { test: ['CMD', 'redis-cli', 'ping'], interval: 5s }`; add `redis-data` to the top-level `volumes:` block
- [ ] In `Dockerfile` lines 5 and 31 change `FROM oven/bun:1` to `FROM oven/bun:1.2.5@sha256:<actual-digest>` for both builder and runtime stages (run `docker pull oven/bun:1.2.5` and `docker inspect --format='{{index .RepoDigests 0}}' oven/bun:1.2.5` to get the digest); after the `COPY` lines add `USER bun` (the official oven/bun image already has a `bun` user with UID 999); verify with `docker run --rm oven/bun:1.2.5 id bun`
- [ ] Create `.dockerignore` at repo root with contents: `.git`, `.gitignore`, `.env`, `.env.*`, `node_modules`, `frontend/node_modules`, `backend/node_modules`, `graphify-out`, `.playwright-mcp`, `test-results`, `*.log`, `Dockerfile*`, `docker-compose*`, `README.md`, `docs/`
- [ ] In `docker-compose.yml` under the `app` service add `cap_drop: ['ALL']`, `security_opt: ['no-new-privileges:true']`, `restart: unless-stopped`, `mem_limit: 512m`, `cpus: '1.0'`, `read_only: true`, `tmpfs: ['/tmp:size=64m']`; add `env_file: - .env.production`; create `.env.production.example` with `JWT_SECRET=changeme`, `REDIS_URL=redis://redis:6379`, `FRONTEND_DEV_URL=http://localhost:3000`; remove the dead `IN_DOCKER=true` env from the `app` service
- [ ] Create `Caddyfile` at repo root with reverse-proxy config: `:443 { tls internal; reverse_proxy app:3000 { health_uri /health health_interval 10s } rate_limit { zone app_rl zone_size 10m key {remote_host} events 100 } } :80 { redir https://{host}{uri} }`
- [ ] In `docker-compose.yml` add a `caddy` service: `image: caddy:2-alpine`, `ports: ['80:80', '443:443']`, `volumes: ['./Caddyfile:/etc/caddy/Caddyfile:ro', 'caddy-data:/data', 'caddy-config:/config']`, `depends_on: [app]`