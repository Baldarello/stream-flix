# Todo
- [ ] Create `frontend/store/navigationStore.js` exporting a MobX class `NavigationStore` with observables `currentActiveView` (string, default `'Home'`), `currentSelectedItem` (object|null), `homePageRows` (array), and actions `setActiveView(view)`, `selectItem(item)`, `clearSelection()`; register in `frontend/store/index.js`
- [ ] In `frontend/store/mediaStore.js` move `nowPlayingItem`, `currentTime`, `duration`, `isPlaying` observables and the `startPlayback`, `stopPlayback`, `togglePlay`, `seek` actions to `frontend/store/playbackStore.js` (existing file, extend it); update all consumers (`frontend/components/media/VideoPlayer.jsx`, `SlaveVideoPlayer.jsx`, `HoloPlayerControls.jsx`, `remoteStore.js`) to import from `playbackStore`
- [ ] In `frontend/store/mediaStore.js` refactor the file to be a ~200L facade that wires together `catalogStore`, `libraryStore`, `playbackStore`, `navigationStore`, and `searchStore`; remove all the moved observables and actions; keep only cross-store coordination logic (e.g. `startPlayback` calling `libraryStore.addToHistory`)
- [ ] Create `frontend/store/dragStateStore.js` exporting a MobX class `DragStateStore` with observables `dragItemId` (string|null), `dropTargetId` (string|null), and actions `startDrag(itemId)`, `setDropTarget(targetId)`, `endDrag()`; register in `frontend/store/index.js`
- [ ] Create `frontend/hooks/useDragState.js` exporting a hook that returns `{ dragItemId, dropTargetId, startDrag, setDropTarget, endDrag }` from `useStores().dragStateStore`; this is the only public API for drag state
- [ ] In `frontend/components/library/MyListDetailView.jsx` lines 114-118 replace the 5 `useState` calls (`filter`, `sort`, `pendingRemove`, `dragItemId`, `dropTargetId`) with `libraryStore.filter`, `libraryStore.sort`, `libraryStore.pendingRemove`, and the new `useDragState` hook
- [ ] In `frontend/components/library/EpisodesDrawer.jsx` replace the local drag state (dragItemId/dropTargetId) with `useDragState()`; remove the corresponding `useState` calls and the local handler
- [ ] In `frontend/components/layout/CinematicRow.jsx` lines 49-52 replace drag `useState` with `useDragState()`; keep the scroll-flag `useState` for `canScrollLeft/canScrollRight` because it is purely visual
- [ ] In `frontend/components/remote/MasterScreen.jsx` lines 15-17 replace `useState` for `scanError` and `scanSuccess` with `remoteStore.scanError` / `remoteStore.scanSuccess` (add the observables to `remoteStore`); keep `manualCode` as `useState` because it is local form state
- [ ] In `frontend/components/library/VideoLinksTab.jsx` line 57 replace `useState` for `expandedShowId` with `libraryStore.expandedShowId` (add the observable) and persist it on toggle
- [ ] In `frontend/services/websocketService.js` lines 104-147 remove all the inline message-type-specific handling from `onmessage`; keep only the raw `events.emit('message', parsed)` so the new `wsRouter` (see next task) becomes the single dispatcher
- [ ] Create `frontend/services/wsRouter.js` exporting `register(storeName, messageType, handler)` and `unregister(storeName, messageType, handler)`; internally subscribes once to `websocketService.events.on('message', ...)` and dispatches to the registered handler based on `message.type` and `storeName`; export `dispatch(message)` for testing
- [ ] In `frontend/store/remoteStore.js` line 76 and `frontend/store/watchTogetherStore.js` line 30 replace the direct `websocketService.events.on('message', ...)` calls with `wsRouter.register('remoteStore', 'quix-slave-registered', handler)` (one register per message type per store)
- [ ] In `frontend/services/websocketService.js` lines 178-195 add a `sendQueue` array (max 100 messages, LRU eviction) to `sendMessage`; if socket is not OPEN, push to queue; on `onopen`, flush the queue; if queue full, drop the oldest with a warning log
- [ ] In `frontend/services/websocketService.js` lines 160-168 in the reconnect loop, change the delay calculation from `Math.min(30000, base * 2 ** attempt)` to `Math.min(30000, base * 2 ** attempt) * (0.8 + Math.random() * 0.4)` to add ±20% jitter and prevent thundering herd
- [ ] In `frontend/services/websocketService.js` line 67 remove the `this.connect()` call from the constructor; export a separate `init()` method and call it from `App.jsx` useEffect mount after `StoreProvider` is ready
- [ ] In `frontend/services/websocketService.js` add a `ws.onpong = () => { this._lastPong = Date.now() }` handler so the client can verify the server's heartbeat is alive (informs `remoteStore.connectionHealth`)
- [ ] In `frontend/services/websocketService.js` lines 149-156 in the `onclose` handler, before emitting `slaves-offline`, check if a reconnect is scheduled within 3s; if so, only emit a `ws-temp-disconnect` event instead of `slaves-offline` to prevent false offline state
- [ ] In `frontend/services/websocketService.js` in the `connect()` method, read `sessionToken` from `db.preferences.get('sessionToken')` and send it in the `Authorization: Bearer <token>` header on the WebSocket upgrade
- [ ] In `frontend/store/remoteStore.js` lines 602-622 rewrite `startMasterReconnectTimer` to subscribe to `wsRouter.events.connected` and cancel the timer when a connection event fires; the timer should only run when `wsRouter.connected === false`
- [ ] In `frontend/store/remoteStore.js` lines 554-587 delete the entire `startPingInterval` function and its caller; the server does not handle `heartbeat-ping` (verified), so this is dead code wasting bandwidth
- [ ] In `frontend/store/remoteStore.js` lines 270-284 in `setSlaveOnlineStatus` and lines 116-120 in `handleSlavesOffline`, add a 3s debounce: if `wsRouter.connected` becomes true within 3s of the offline event, revert the slave's `isOnline` flag back to true
- [ ] In `frontend/store/remoteStore.js` lines 746-873 in `handleIncomingMessage`, remove the legacy `playback-status` handler (lines around 834); keep only `quix-slave-status-update` as the single source of truth for slave state
- [ ] In `frontend/store/mediaStore.js` search for any code that writes to `remoteSlaveState` and remove it; move those writes to `frontend/store/remoteStore.js` so there is a single owner of the slave state
- [ ] In `frontend/store/remoteStore.js` in `sendPlayCommandAndOptimisticallyUpdate` and `sendRemoteCommand('play'|'pause')`, on receipt of `quix-slave-not-connected` or `quix-command-failed` within 5s, revert the optimistic update and surface a snackbar via `uiStore.notify`
- [ ] In `frontend/store/remoteStore.js` lines 642-683 in `syncMediaFromMaster`, wrap the loop in `await db.transaction('rw', db.cachedItems, db.myList, db.mediaLinks, async () => { ... })`; replace sequential `await put` with `await db.cachedItems.bulkPut(batch)` per 100 items
- [ ] In `frontend/store/remoteStore.js` add a new `groupControl` observable `Map<groupId, slaveIds[]>` and actions `createGroup(name)`, `addSlaveToGroup(groupId, slaveId)`, `removeSlaveFromGroup`, `broadcastToGroup(groupId, command)`; register in the websocketService to send `quix-group-control` on broadcast
- [ ] In `frontend/services/db.js` bump the top version from 13 to 14 and add the new tables: `profiles` (`&id, name`), `myLists` (`&id, name, order`), `listItems` (`++id, [listId+mediaId], listId, mediaId`), `searchHistory` (`++id, query, usedAt`), `downloadedBlobs` (`&mediaId, downloadedAt`), `tags` (`&id, name`), `collectionRules` (`&id, name`)
- [ ] In `frontend/services/db.js` lines 75-79 replace `tx.table('episodeLinks').clear()` with a batched delete: `let last = 0; while ((last = await tx.table('episodeLinks').where('id').above(last).limit(1000).primaryKeys()) && last.length) await tx.table('episodeLinks').bulkDelete(last)` to avoid blocking the upgrade
- [ ] In `frontend/services/db.js` line 108 change the `myList` schema from `'&id, order'` to `'&id, order, [order+addedAt]'` to support range queries on order
- [ ] In `frontend/services/db.js` line 131 change `knownSlaves` schema from `'&id, lastSeen'` to `'&id, lastSeen, shortCode'` (re-add the shortCode index that was lost in v11)
- [ ] In `frontend/services/db.js` line 139 change `mediaLinks` schema from `'++id, mediaId, isValid'` to `'++id, mediaId, isValid, [mediaId+isValid]'` for compound lookups
- [ ] In `frontend/services/db.js` line 135 change `episodeProgress` schema from `'&episodeId, lastWatchedAt'` to `'&episodeId, lastWatchedAt, [episodeId+lastWatchedAt]'`
- [ ] In `frontend/services/db.js` line 62 change `viewingHistory` schema from `'++id, episodeId, watchedAt'` to `'++id, episodeId, watchedAt, [episodeId+watchedAt]'`
- [ ] In `frontend/services/db.js` lines 148-159 the dead `handleDbChanges` function, wire it up with `this.on('changes', handleDbChanges)` inside the version upgrade so it logs changes in dev mode
- [ ] In `frontend/services/db.js` version 8 upgrade, before the `oldMyList` rewrite, check `if (oldMyList.length > 0 && oldMyList[0].order !== undefined) return` to make the migration idempotent on re-run
- [ ] Create `frontend/store/profileStore.js` exporting a MobX class `ProfileStore` with observables `activeProfileId` (string|null), `profiles` (array), and computeds `isKidProfile`, `activeProfile`; actions `setActiveProfile(id)`, `createProfile({ name, avatar, isKid, pin })`, `deleteProfile(id)`, `verifyPin(pin)`; persist to `db.profiles`
- [ ] In `frontend/store/playbackStore.js` add observables `sleepTimerMinutes` (number|null), `sleepTimerEndAt` (number|null); actions `setSleepTimer(min)` (sets a `setTimeout` that calls `togglePlay` when fires), `clearSleepTimer()`; computed `sleepTimerRemainingSec`
- [ ] In `frontend/store/playbackStore.js` add observable `upNext` (array); action `enqueueUpNext(item)`, `removeFromUpNext(itemId)`, `clearUpNext()`; persist nothing (session-only)
- [ ] In `frontend/store/mediaStore.js` add a computed `lastWatched` that returns `this.continueWatchingItems[0]` or null; expose via `useStores().mediaStore.lastWatched`
- [ ] In `frontend/store/searchStore.js` add observable `recentSearches` (array, max 10, persisted in `db.searchHistory`), `suggestions` (array, debounced), `voiceSupported` (boolean from `'webkitSpeechRecognition' in window`); actions `addRecent(query)`, `clearRecent()`, `fetchSuggestions(prefix)`
- [ ] In `frontend/store/libraryStore.js` add observable `myLists` (array of `{ id, name, items, order }`); actions `createList(name)`, `deleteList(id)`, `addItemToList(listId, mediaId)`, `removeItemFromList`, `reorderList`, `reorderItems(listId, fromIdx, toIdx)`; persist in `db.myLists` + `db.listItems`
- [ ] In `frontend/store/libraryStore.js` add computeds: `unwatchedMovies` (movies in myList with no `watchHistory` entry), `unfinishedSeries` (series with at least one unwatched episode), `addedThisMonth` (items added in the last 30 days), `trendingInMyList` (top 5 by `watchHistory.length` in last 7 days)
- [ ] In `frontend/store/preferencesStore.js` add observables `accentHue` (number 0-360, default 220), `density` ('compact'|'normal'|'spacious', default 'normal'), `fontSize` (number 12-20, default 16); actions `setAccentHue`, `setDensity`, `setFontSize`; persist all to `db.preferences`; on change, set `document.documentElement.style.setProperty('--accent', \`hsl(\${hue} 70% 50%)\`)`
- [ ] In `frontend/App.jsx` line 22 replace the direct `websocketService.connect()` call with `useEffect(() => { remoteStore.bootstrap() }, [])` which internally calls `websocketService.init()` and registers all stores via `wsRouter`
- [ ] In `frontend/App.jsx` lines 148-158 wrap the `window.__quixTest = ...` assignment in `if (import.meta.env.DEV)` so the test surface is stripped from production bundles
- [ ] In `frontend/App.jsx` add a `useEffect` mount that checks `profileStore.profiles.length === 0` and renders the `ProfileSwitcherModal` in create-mode; otherwise renders the normal app
- [ ] In `frontend/App.jsx` add a `useEffect` mount that calls `syncStore.scheduleAutoBackup()` which sets a daily `setInterval` calling `shareService.createShareLink()` + `googleDriveService.uploadBackup()`
