# Todo
- [ ] Create `frontend/store/navigationStore.js` exporting a MobX class `NavigationStore` with observables `currentActiveView` (string, default `'Home'`), `currentSelectedItem` (object|null), `homePageRows` (array), and actions `setActiveView(view)`, `selectItem(item)`, `clearSelection()`; register in `frontend/store/index.js`
- [ ] In `frontend/store/mediaStore.js` move `nowPlayingItem`, `currentTime`, `duration`, `isPlaying` observables and the `startPlayback`, `stopPlayback`, `togglePlay`, `seek` actions to `frontend/store/playbackStore.js` (existing file, extend it); update all consumers (`frontend/components/media/VideoPlayer.jsx`, `SlaveVideoPlayer.jsx`, `HoloPlayerControls.jsx`, `remoteStore.js`) to import from `playbackStore`; refactor the file to be a ~200L facade that wires together `catalogStore`, `libraryStore`, `playbackStore`, `navigationStore`, and `searchStore`; remove all the moved observables and actions; keep only cross-store coordination logic (e.g. `startPlayback` calling `libraryStore.addToHistory`); add computed `lastWatched` that returns `this.continueWatchingItems[0]` or null; expose via `useStores().mediaStore.lastWatched`; search for any code that writes to `remoteSlaveState` and remove it; move those writes to `frontend/store/remoteStore.js` so there is a single owner of the slave state; add `cachedItems[id].meta` object with `{ reviews, providers, recommendations, fetchedAt }`; cache for 24h before refetching; add a `getRecommendedFor(item)` method that calls `apiCall.getRecommendations(item.id, item.type)` and returns the first 10 results, cached in `cachedItems[item.id].meta.recommendations` for 24h — all in one sequential line
- [ ] Create `frontend/store/dragStateStore.js` exporting a MobX class `DragStateStore` with observables `dragItemId` (string|null), `dropTargetId` (string|null), and actions `startDrag(itemId)`, `setDropTarget(targetId)`, `endDrag()`; register in `frontend/store/index.js`
- [ ] Create `frontend/hooks/useDragState.js` exporting a hook that returns `{ dragItemId, dropTargetId, startDrag, setDropTarget, endDrag }` from `useStores().dragStateStore`; this is the only public API for drag state
- [ ] In `frontend/store/playbackStore.js` add observables `sleepTimerMinutes` (number|null), `sleepTimerEndAt` (number|null); actions `setSleepTimer(min)` (sets a `setTimeout` that calls `togglePlay` when fires), `clearSleepTimer()`; computed `sleepTimerRemainingSec`; add observable `upNext` (array); action `enqueueUpNext(item)`, `removeFromUpNext(itemId)`, `clearUpNext()`; persist nothing (session-only)
- [ ] Create `frontend/store/profileStore.js` exporting a MobX class `ProfileStore` with observables `activeProfileId` (string|null), `profiles` (array), and computeds `isKidProfile`, `activeProfile`; actions `setActiveProfile(id)`, `createProfile({ name, avatar, isKid, pin })`, `deleteProfile(id)`, `verifyPin(pin)`; persist to `db.profiles`; add `verifyPin(pin)` action that compares against the hashed PIN stored in the profile row (use `crypto.subtle.digest('SHA-256', ...)` to hash before comparing); in `createProfile` hash the PIN, store the profile, persist to `db.profiles`
- [ ] In `frontend/store/searchStore.js` add observable `recentSearches` (array, max 10, persisted in `db.searchHistory`), `suggestions` (array, debounced), `voiceSupported` (boolean from `'webkitSpeechRecognition' in window`); actions `addRecent(query)`, `clearRecent()`, `fetchSuggestions(prefix)`; add `addRecent(query)` that does `await db.searchHistory.add({ query, usedAt: Date.now() })` and prunes to 10 entries
- [ ] In `frontend/store/libraryStore.js` add observable `myLists` (array of `{ id, name, items, order }`); actions `createList(name)`, `deleteList(id)`, `addItemToList(listId, mediaId)`, `removeItemFromList`, `reorderList`, `reorderItems(listId, fromIdx, toIdx)`; persist in `db.myLists` + `db.listItems`; add computeds: `unwatchedMovies` (movies in myList with no `watchHistory` entry), `unfinishedSeries` (series with at least one unwatched episode), `addedThisMonth` (items added in the last 30 days), `trendingInMyList` (top 5 by `watchHistory.length` in last 7 days); make every read of `myList`, `watchHistory`, and `continueWatching` filter by `profileStore.activeProfileId`; add migration code to backfill existing rows with `activeProfileId = null` (treated as the default profile); add `addTag(itemId, tagName)`, `removeTag`, `getItemsByTag` that use the `tags` table; add `evaluateRules()` that runs every rule against the library and produces a `{ ruleId, itemIds }[]` cached observable
- [ ] In `frontend/store/preferencesStore.js` add observables `accentHue` (number 0-360, default 220), `density` ('compact'|'normal'|'spacious', default 'normal'), `fontSize` (number 12-20, default 16); actions `setAccentHue`, `setDensity`, `setFontSize`; persist all to `db.preferences`; on change, set `document.documentElement.style.setProperty('--accent', \`hsl(\${hue} 70% 50%)\`)`
- [ ] In `frontend/store/remoteStore.js` add a new `groupControl` observable `Map<groupId, slaveIds[]>` and actions `createGroup(name)`, `addSlaveToGroup(groupId, slaveId)`, `removeSlaveFromGroup`, `broadcastToGroup(groupId, command)`; register in the websocketService to send `quix-group-control` on broadcast; lines 602-622 rewrite `startMasterReconnectTimer` to subscribe to `wsRouter.events.connected` and cancel the timer when a connection event fires; the timer should only run when `wsRouter.connected === false`; lines 554-587 delete the entire `startPingInterval` function and its caller; the server does not handle `heartbeat-ping` (verified), so this is dead code wasting bandwidth; lines 270-284 in `setSlaveOnlineStatus` and lines 116-120 in `handleSlavesOffline`, add a 3s debounce: if `wsRouter.connected` becomes true within 3s of the offline event, revert the slave's `isOnline` flag back to true; lines 746-873 in `handleIncomingMessage`, remove the legacy `playback-status` handler (lines around 834); keep only `quix-slave-status-update` as the single source of truth for slave state; in `sendPlayCommandAndOptimisticallyUpdate` and `sendRemoteCommand('play'|'pause')`, on receipt of `quix-slave-not-connected` or `quix-command-failed` within 5s, revert the optimistic update and surface a snackbar via `uiStore.notify`; lines 642-683 in `syncMediaFromMaster`, wrap the loop in `await db.transaction('rw', db.cachedItems, db.myList, db.mediaLinks, async () => { ... })`; replace sequential `await put` with `await db.cachedItems.bulkPut(batch)` per 100 items; `triggerAutoFullscreen()` add a guard `if (/iPhone|iPad/.test(navigator.userAgent)) return` because iOS Safari does not support programmatic fullscreen from JS — all in one sequential line
- [ ] In `frontend/store/watchTogetherStore.js` on the `quix-join-room` handler, send `{ type: 'quix-join-room', lastKnownTime: playbackStore.currentTime }`; set a `setInterval(5000)` that pings the room with `playbackStore.currentTime` for late-join sync
- [ ] Create `frontend/store/downloadStore.js` (new) with `downloadItem(mediaId, url)` that POSTs to `/api/fetch-blob { url }`, receives the blob stream, saves it with `db.downloadedBlobs.put({ mediaId, blob, size, downloadedAt: Date.now() })`; add a daily cleanup: a `setInterval` registered in `App.jsx` mount that calls `db.downloadedBlobs.where('downloadedAt').below(Date.now() - 30*24*3600*1000).delete()` if the user's `preferencesStore.autoCleanupDownloads` is true
- [ ] Create `frontend/store/syncStore.js` with observables `autoBackupEnabled` (boolean), `lastBackupAt` (timestamp); action `setAutoBackupEnabled(bool)`; on `App.jsx` mount, if `autoBackupEnabled`, schedule a daily `setInterval` that calls `googleDriveService.uploadBackup(shareService.exportLibrary())` and updates `lastBackupAt`
- [ ] In `frontend/services/websocketService.js` lines 104-147 remove all the inline message-type-specific handling from `onmessage`; keep only the raw `events.emit('message', parsed)` so the new `wsRouter` becomes the single dispatcher; lines 178-195 add a `sendQueue` array (max 100 messages, LRU eviction) to `sendMessage`; if socket is not OPEN, push to queue; on `onopen`, flush the queue; if queue full, drop the oldest with a warning log; lines 160-168 in the reconnect loop, change the delay calculation from `Math.min(30000, base * 2 ** attempt)` to `Math.min(30000, base * 2 ** attempt) * (0.8 + Math.random() * 0.4)` to add ±20% jitter and prevent thundering herd; line 67 remove the `this.connect()` call from the constructor; export a separate `init()` method and call it from `App.jsx` useEffect mount after `StoreProvider` is ready; add a `ws.onpong = () => { this._lastPong = Date.now() }` handler so the client can verify the server's heartbeat is alive; lines 149-156 in the `onclose` handler, before emitting `slaves-offline`, check if a reconnect is scheduled within 3s; if so, only emit a `ws-temp-disconnect` event instead of `slaves-offline`; in the `connect()` method, read `sessionToken` from `db.preferences.get('sessionToken')` and send it in the `Authorization: Bearer <token>` header on the WebSocket upgrade; add a `LOG_LEVEL` env read in the constructor and wrap all `console.log/error/warn` calls in `if (LOG_LEVEL === 'debug')`; expose `setLogLevel(level)` for dev — all in one sequential line
- [ ] Create `frontend/services/wsRouter.js` exporting `register(storeName, messageType, handler)` and `unregister(storeName, messageType, handler)`; internally subscribes once to `websocketService.events.on('message', ...)` and dispatches to the registered handler based on `message.type` and `storeName`; export `dispatch(message)` for testing
- [ ] In `frontend/store/remoteStore.js` line 76 and `frontend/store/watchTogetherStore.js` line 30 replace the direct `websocketService.events.on('message', ...)` calls with `wsRouter.register('remoteStore', 'quix-slave-registered', handler)` (one register per message type per store)
- [ ] In `frontend/services/db.js` bump the top version from 13 to 14 and add the new tables: `profiles` (`&id, name`), `myLists` (`&id, name, order`), `listItems` (`++id, [listId+mediaId], listId, mediaId`), `searchHistory` (`++id, query, usedAt`), `downloadedBlobs` (`&mediaId, downloadedAt`), `tags` (`&id, name`), `collectionRules` (`&id, name`); lines 75-79 replace `tx.table('episodeLinks').clear()` with a batched delete: `let last = 0; while ((last = await tx.table('episodeLinks').where('id').above(last).limit(1000).primaryKeys()) && last.length) await tx.table('episodeLinks').bulkDelete(last)` to avoid blocking the upgrade; line 108 change the `myList` schema from `'&id, order'` to `'&id, order, [order+addedAt]'` to support range queries on order; line 131 change `knownSlaves` schema from `'&id, lastSeen'` to `'&id, lastSeen, shortCode'` (re-add the shortCode index that was lost in v11); line 139 change `mediaLinks` schema from `'++id, mediaId, isValid'` to `'++id, mediaId, isValid, [mediaId+isValid]'` for compound lookups; line 135 change `episodeProgress` schema from `'&episodeId, lastWatchedAt'` to `'&episodeId, lastWatchedAt, [episodeId+lastWatchedAt]'`; line 62 change `viewingHistory` schema from `'++id, episodeId, watchedAt'` to `'++id, episodeId, watchedAt, [episodeId+watchedAt]'`; lines 148-159 the dead `handleDbChanges` function, wire it up with `this.on('changes', handleDbChanges)` inside the version upgrade so it logs changes in dev mode; version 8 upgrade, before the `oldMyList` rewrite, check `if (oldMyList.length > 0 && oldMyList[0].order !== undefined) return` to make the migration idempotent on re-run — all in one sequential line
- [ ] In `frontend/App.jsx` line 22 replace the direct `websocketService.connect()` call with `useEffect(() => { remoteStore.bootstrap() }, [])` which internally calls `websocketService.init()` and registers all stores via `wsRouter`; lines 148-158 wrap the `window.__quixTest = ...` assignment in `if (import.meta.env.DEV)` so the test surface is stripped from production bundles; add a `useEffect` mount that checks `profileStore.profiles.length === 0` and renders the `ProfileSwitcherModal` in create-mode; otherwise renders the normal app; add a `useEffect` mount that calls `syncStore.scheduleAutoBackup()` which sets a daily `setInterval` calling `shareService.createShareLink()` + `googleDriveService.uploadBackup()`; add `<MoodLobby />` route accessible from the home hero as a "Pick a mood" button that opens a fullscreen modal — all in one sequential line