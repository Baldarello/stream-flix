# Todo
- [ ] In `frontend/services/apiCall.jsx` add `getReviews(id, type)` that calls TMDB `/movie/{id}/reviews` or `/tv/{id}/reviews` based on `type`, returns `{ id, author, content, rating, createdAt }[]`, with a 24h cache key; add `getWatchProviders(id, type)` that calls TMDB `/movie/{id}/watch/providers` or `/tv/{id}/watch/providers`, returns `{ country, flatrate?, rent?, buy? }[]` keyed by ISO country code; add `getRecommendations(id, type)` that calls TMDB `/movie/{id}/recommendations` or `/tv/{id}/recommendations`, returns TMDB `results` array (limited to 20), with a 24h cache; add `searchSuggest(prefix)` that calls TMDB `/search/multi?query=${prefix}&include_adult=false`, returns the first 5 results with `{ id, mediaType, title, posterPath }`; add `getTrendingByGenre(genreId, type='movie')` that calls TMDB `/discover/${type}?with_genres=${genreId}&sort_by=popularity.desc`, returns the first 20 results; add `getSeasonRecap(seasonId)` that fetches `/tv/{seriesId}/season/{seasonNumber}` from TMDB and returns `{ name, overview, episodeCount, airDate }`; refactor the fetch helpers to support a `retries: 3, backoffMs: 500` option that wraps the fetch in an exponential backoff loop (500ms, 1000ms, 2000ms) and only retries on 5xx and network errors — all in one sequential line
- [ ] In `frontend/services/googleAuthService.js` add a `getSessionToken(googleIdToken)` method that POSTs to `/api/session` with `{ googleIdToken }` and stores the returned `{ token, expiresAt }` in `db.preferences.sessionToken`; replace the current local `scheduleTokenRefresh` with one that uses the backend-issued JWT expiry (`expiresAt`) and refreshes by re-running the Google OAuth silent flow; on 401, exponential backoff and retry; add a `signOut()` method that calls `POST /api/session/revoke` (new), clears `db.preferences.sessionToken`, calls `websocketService.disconnect()`, and resets all stores
- [ ] In `frontend/services/linkService.js` add a `language` field (string, default 'en') to every `mediaLinks` row; update the add/edit forms to expose a language picker; add a `subtitleUrl` field (string, optional) to every `mediaLinks` row; the `HoloPlayerControls` captions menu uses it to load a `<track kind="subtitles">`; add `preferredSources` (array of source hostnames) per user and per media, with a UI to rank them; the `playItem` action picks the highest-ranked valid link; add bulk operations: `bulkAddLinks(mediaId, urls[])` and `bulkAddEpisodeLinks(showId, seasonEpisodeMap, urls[])` that wrap multiple `addLink` calls in a single Dexie transaction; add link-expiry tracking: when a link returns 4xx during playback, mark `isValid=false` and emit a `link-expired` event that the `NotificationsModal` listens to and shows a snackbar
- [ ] In `frontend/services/linkValidator.js` rewrite the validation as an async function that does a HEAD request with a 5s timeout, checks `Content-Type` starts with `video/` or `application/`, and returns `{ valid, status, contentType, finalUrl }`
- [ ] Create `frontend/components/modals/ProfileSwitcherModal.jsx` as a full-screen modal that lists all profiles (avatar + name), has a "+ New profile" button, and switches `profileStore.activeProfileId` on click
- [ ] Create `frontend/components/modals/PinEntryDialog.jsx` as a numeric pad modal (4-digit) that calls `profileStore.verifyPin(pin)` and resolves on success; used to gate kid-profile exits
- [ ] In `frontend/store/mediaStore.js` add a `filteredByKidProfile` computed that, when `profileStore.isKidProfile` is true, filters out items with `voteAverage < 6.5` and genres ['Horror', 'Thriller', 'Crime']; the `homePageRows` and search results apply this filter
- [ ] In `frontend/components/library/LibraryDashboard.jsx` add a "Profile" chip in the header that shows the active profile avatar and name, opens `ProfileSwitcherModal` on click
- [ ] Add `vite-plugin-pwa` to `frontend/package.json` devDependencies and run `bun install`
- [ ] Create `frontend/public/manifest.webmanifest` with `name: "Quix"`, `short_name: "Quix"`, `start_url: "/"`, `display: "standalone"`, `background_color: "#000000"`, `theme_color: "#1a1a1a"`, `icons: [{ src: "/icons/icon-192.png", sizes: "192x192", type: "image/png" }, { src: "/icons/icon-512.png", sizes: "512x512", type: "image/png" }, { src: "/icons/icon-maskable.png", sizes: "512x512", type: "image/png", purpose: "maskable" }]`
- [ ] Create `frontend/public/icons/icon-192.png`, `icon-512.png`, `icon-maskable.png` (placeholder 1x1 PNGs acceptable initially, real icons later)
- [ ] Create `frontend/public/sw.js` with three Workbox strategies: `registerRoute(/\/api\/tmdb\/.*\/images/, new StaleWhileRevalidate({ cacheName: 'tmdb-images' }))`, `registerRoute(/^\/$|^\/index\.html/, new CacheFirst({ cacheName: 'app-shell' }))`, `registerRoute(/\/api\//, new NetworkOnly())`; import Workbox from a CDN or bundle it; add `self.addEventListener('message', (e) => { if (e.data.type === 'SKIP_WAITING') self.skipWaiting() })` and a frontend listener that detects `sw.updateFound` and shows a snackbar "New version available, reload?"; ensure the cache strategies are: app shell `CacheFirst`, TMDB posters `StaleWhileRevalidate`, API calls `NetworkOnly` — all in one sequential line
- [ ] Create `frontend/components/system/OfflineBanner.jsx` that listens to `online`/`offline` window events and renders a fixed-bottom Snackbar with `t('system.offline')` when offline; `id="offline-banner"`
- [ ] In `frontend/components/layout/Header.jsx` add an "Install app" button that appears when `window.deferredPrompt` is set (from `beforeinstallprompt`); clicking calls `prompt.prompt()` and shows the result
- [ ] In `frontend/store/downloadStore.js` (new) add `downloadItem(mediaId, url)` that POSTs to `/api/fetch-blob { url }`, receives the blob stream, saves it with `db.downloadedBlobs.put({ mediaId, blob, size, downloadedAt: Date.now() })`; add a daily cleanup: a `setInterval` registered in `App.jsx` mount that calls `db.downloadedBlobs.where('downloadedAt').below(Date.now() - 30*24*3600*1000).delete()` if the user's `preferencesStore.autoCleanupDownloads` is true
- [ ] In `frontend/components/library/LibraryDashboard.jsx` on each card add a small "downloaded" badge (cloud-check icon) if `db.downloadedBlobs.get(item.id)` returns a row, plus a download icon button that triggers `downloadStore.downloadItem(item.id, item.video_url)`; add a download progress UI: a thin bar at the bottom of the card that shows the `ReadableStream` progress from the `/api/fetch-blob` response
- [ ] In `frontend/services/shareService.js` add `shareList(listId)` that exports the list (with items and links) as JSON, uploads to Drive, and returns a shareable URL
- [ ] In `frontend/services/googleDriveService.js` add `uploadBackup(json)`, `listBackups()`, `restoreBackup(fileId)` that use the Google Drive API v3 with the user's access token
- [ ] In `frontend/store/syncStore.js` add observables `autoBackupEnabled` (boolean), `lastBackupAt` (timestamp); action `setAutoBackupEnabled(bool)`; on `App.jsx` mount, if `autoBackupEnabled`, schedule a daily `setInterval` that calls `googleDriveService.uploadBackup(shareService.exportLibrary())` and updates `lastBackupAt`
- [ ] In `frontend/views/system/PreferencesView.jsx` add a "Sync" tab that shows `autoBackupEnabled` toggle, `lastBackupAt`, and a "Backup now" button; create the file as a `<Tabs>` view with tabs: Theme (ThemeTab), Playback (PlaybackTab — auto-play, default quality, skip intro), Library (LibraryTab — default view, sort order), Sync (SyncTab — auto-backup toggle)
- [ ] In `frontend/components/feedback/NotificationsModal.jsx` listen to the new `backup-completed` event from `syncStore` and show a success toast with a "View in Drive" link
- [ ] In `frontend/store/libraryStore.js` (frontend-stores 4.6.7) the smart collections computeds are already defined; expose them as `libraryStore.unwatchedMovies` etc. so `LibraryDashboard.jsx` can render them; in `frontend/components/library/LibraryDashboard.jsx` render a chips row above the manual lists: for each rule, a chip with the rule name and item count; clicking filters the grid to that rule's items — all in one sequential line
- [ ] Create `frontend/components/library/SmartCollectionRule.jsx` as a small form that lets the user define a rule with conditions: `genre IN [...]`, `rating >= N`, `year >= N`, `watchedStatus IN ['unwatched', 'in-progress', 'finished']`, `addedAt >= N days ago`; persists to `db.collectionRules`
- [ ] In `frontend/services/db.js` (after v14 bump) the `collectionRules` table is defined; in `frontend/store/libraryStore.js` add `evaluateRules()` that runs every rule against the library and produces a `{ ruleId, itemIds }[]` cached observable
- [ ] In `frontend/store/libraryStore.js` (frontend-stores 4.6.6) `myLists` is already defined; in `frontend/components/library/MyListDetailView.jsx` add a dropdown to switch the active list and a drag handle to drag items between lists
- [ ] In `frontend/services/db.js` (after v14 bump) `myLists`, `listItems`, and `tags` tables are defined; in `frontend/store/libraryStore.js` add `addTag(itemId, tagName)`, `removeTag`, `getItemsByTag` that use the `tags` table
- [ ] In `frontend/store/preferencesStore.js` (frontend-stores 4.6.8) `accentHue`, `density`, `fontSize` are already defined; in `frontend/components/settings/ThemeTab.jsx` add a color picker bound to `accentHue`, a density radio (compact/normal/spacious), and a font-size slider
- [ ] Create `frontend/components/settings/PlaybackTab.jsx`, `LibraryTab.jsx`, `SyncTab.jsx` as small form components that bind to `preferencesStore` and `syncStore`
- [ ] In `frontend/styles/themePalettes.js` add a `highContrastPalette` object that uses `#FFFFFF` on `#000000` for text and `#FFFF00` for accents; switch the active palette when `window.matchMedia('(prefers-contrast: more)').matches`
- [ ] In `frontend/store/watchTogetherStore.js` on the `quix-join-room` handler, send `{ type: 'quix-join-room', lastKnownTime: playbackStore.currentTime }`; set a `setInterval(5000)` that pings the room with `playbackStore.currentTime` for late-join sync
- [ ] In `frontend/components/media/VideoPlayer.jsx` add a `ReactionsLayer` overlay that listens to `remoteStore.reactions` and renders each emoji as a floating bubble for 2s, with a CSS animation that respects `prefers-reduced-motion`
- [ ] In `frontend/components/modals/WatchTogetherModal.jsx` add a "Vote for next" section that shows the top 3 candidates (from `upNext`) and lets each participant tap a heart; tally is synced via `quix-vote` and the winner is auto-enqueued in `upNext`; refactor the 412L file into `WatchTogetherRoom.jsx` (room list, participants), `WatchTogetherChat.jsx` (chat + reactions), `WatchTogetherControls.jsx` (host controls, voting) if the file exceeds 600L after this task
- [ ] In `frontend/store/mediaStore.js` add a `getRecommendedFor(item)` method that calls `apiCall.getRecommendations(item.id, item.type)` and returns the first 10 results, cached in `cachedItems[item.id].meta.recommendations` for 24h
- [ ] Create `frontend/components/modals/RecapModal.jsx` that triggers when opening a series episode where `episode.season > 1 && episode.number === 1`; renders a 30-sec auto-scrolling text summary built from `apiCall.getSeasonRecap(previousSeasonId)` and `cachedItems[item.id].overview`
- [ ] Create `frontend/components/library/MoodLobby.jsx` as a grid of mood cards ("Cozy Night", "Horror Marathon", "Feel-Good", "Mind-Bending") each linking to a filtered grid of items from `catalogStore.itemsByMood(moodId)` which calls `apiCall.getTrendingByGenre(genreId)`
- [ ] In `frontend/services/apiCall.jsx` add `getSeasonRecap(seasonId)` that fetches `/tv/{seriesId}/season/{seasonNumber}` from TMDB and returns `{ name, overview, episodeCount, airDate }`
- [ ] In `frontend/App.jsx` add a `<MoodLobby />` route accessible from the home hero as a "Pick a mood" button that opens a fullscreen modal