# Todo
- [x] In `frontend/playwright.config.js` add `baseURL: 'http://localhost:3000'`, `use: { trace: 'on-first-retry', screenshot: 'only-on-failure', video: 'retain-on-failure' }`, `timeout: 30000`, and a `webServer` block that runs `docker compose up -d --build` then waits for `/health` to return 200
- [x] Add `axe-core` and `@axe-core/playwright` to `frontend/package.json` devDependencies and run `bun install`
- [x] Create `frontend/e2e/utils/a11y-helper.js` exporting `expectNoA11yViolations(page, { excludedTags: ['region'] })` that runs `await new AxeBuilder({ page }).analyze()` and asserts zero `critical` or `serious` violations, with a clear error message listing the failing nodes
- [x] Create `frontend/e2e/utils/db-reset.js` exporting `resetIndexedDB(page)` that calls `indexedDB.deleteDatabase('quix-db')` in every open tab/worker and reloads the page, plus `seedIndexedDB(page, fixtures)` for fixture loading
- [x] Create `frontend/e2e/utils/ws-mock.js` exporting `mockWebSocket(page, handlers)` that injects a fake `WebSocket` constructor into the page before app load, returning a controllable mock for unit-style WS tests
- [x] Create `.github/workflows/test.yml` that on push/PR runs `docker compose up -d --build`, waits for health, runs `bunx playwright test frontend/e2e/`, and uploads `.playwright-mcp/` and `test-results/` artifacts
- [x] Create `frontend/e2e/smoke.spec.js` Playwright test that navigates to `http://localhost:3000/`, logs in as guest, waits for `id="screen-home"`, asserts no `console.error` events were emitted, takes a screenshot, and exits
- [x] Create `frontend/e2e/invariants/ids-on-roots.spec.js` Playwright test that for each route (home, search, library, settings, detail, player) asserts the topmost `<main>` or `<section>` has an `id` attribute, and fails with the list of routes missing an id
- [x] Create `frontend/e2e/invariants/mobx-no-usestate.spec.js` test that spawns a Node subprocess running `grep -rn "useState" frontend/views/ frontend/features/ frontend/components/ --include="*.jsx" | grep -v "useState(() =>"` and asserts exit code 0 (no results) for screen-level files
- [ ] Create `frontend/e2e/invariants/no-direct-store-imports.spec.js` test that runs `grep -rn "from.*store/[a-zA-Z]*Store" frontend/views/ frontend/components/` and asserts no direct store imports outside of `frontend/store/`, `frontend/services/`, `frontend/context/`
- [ ] Create `frontend/e2e/invariants/i18n-no-hardcoded.spec.js` test that visits every route with `preferencesStore.setLanguage('en')`, then runs `grep -rEn "['\"]([A-Z][a-z]+ )+[a-z]+['\"]" frontend/components/ frontend/views/ frontend/features/` and asserts no hardcoded user-facing strings in JSX
- [ ] Create `frontend/e2e/invariants/no-oversized-files.spec.js` test that reads every `.jsx` and `.js` file under `frontend/components/`, `frontend/store/`, `frontend/services/`, counts lines, and asserts none exceed 800 lines (unless they start with `// @allow-large-file`)
- [ ] Create `frontend/e2e/invariants/a11y-baseline.spec.js` test that for each of [home, search, library, settings, detail, player, modals] calls `expectNoA11yViolations(page)` and aggregates results, failing the suite on any critical/serious axe violation
- [ ] Create `frontend/e2e/invariants/reduced-motion.spec.js` test that uses `page.emulateMedia({ reducedMotion: 'reduce' })`, opens home, and asserts no element has `animation-name !== 'none'` and no GSAP timeline is running (via `window.gsap?.globalTimeline?.getChildren()?.length === 0`)
- [ ] Create `frontend/e2e/invariants/contrast-mode.spec.js` test that uses `page.emulateMedia({ forcedColors: 'active' })`, opens home, and asserts the high-contrast palette is applied by checking `getComputedStyle(document.body).getPropertyValue('--contrast-mode') === 'high'`
- [ ] Create `scripts/verify.sh` (bash) that runs in order: `docker build -t stream-flix .` → `docker compose down` → `docker compose up -d --build` → wait 15s → `docker logs --tail 200 app 2>&1 | grep -E "(ERROR|FATAL)"` fails if any match → `bunx playwright test frontend/e2e/smoke.spec.js` → `bunx graphify export callflow-html`; then create `.husky/pre-commit` running `bun run lint && bunx vitest run --reporter=dot`, add `"verify": "bash scripts/verify.sh"` to `package.json`, create `.github/workflows/verify.yml` on pull_request running `bun run verify` on self-hosted runner, and create `frontend/e2e/verify-script.spec.js` that runs verify.sh in subprocess asserting exit 0 within 5min — all in one sequential line
- [ ] Create `frontend/e2e/feature-sleep-timer.spec.js` test that opens a video, clicks the sleep timer, selects 30 minutes, asserts `playbackStore.sleepTimerMinutes === 30`, fast-forwards by mocking `Date.now`, asserts the video pauses
- [ ] Create `frontend/e2e/feature-pwa-install.spec.js` test that runs Lighthouse PWA audit and asserts score >= 90, manifest is valid, service worker is registered, and `beforeinstallprompt` event fires on home
- [ ] Create `frontend/e2e/feature-pwa-shell.spec.js` test that registers the service worker, then sets `navigator.onLine = false` via `context.setOffline(true)`, reloads, and asserts the offline banner shows plus the last-cached grid is rendered
- [ ] Create `frontend/e2e/feature-multi-profile.spec.js` test that creates two profiles, switches between them, asserts `myList` is independent per profile, asserts `activeProfileId` is persisted to IDB
- [ ] Create `frontend/e2e/feature-profile-pin.spec.js` test that creates a kid profile with PIN 1234, attempts to play an R-rated item, asserts PIN dialog appears, enters wrong PIN (asserts error), enters correct PIN (asserts play proceeds or blocks based on rating)
- [ ] Create `frontend/e2e/feature-search-autocomplete.spec.js` test that types "in" in the search input, asserts a debounced dropdown of suggestions appears within 300ms, clicks one, asserts the result grid loads that item
- [ ] Create `frontend/e2e/feature-quick-resume.spec.js` test that plays a video for 30s, pauses, navigates to home, asserts the `ResumeHero` shows the same item with a progress ring at 6%, clicks it, asserts playback resumes at 30s
- [ ] Create `frontend/e2e/feature-upnext.spec.js` test that starts episode 1 of a series, fast-forwards to 95% of duration, asserts `UpNextTray` appears with episode 2, clicks it, asserts episode 2 plays; then create `frontend/e2e/feature-upnext-swipe.spec.js` (SEQ after upnext) that opens UpNextTray, swipes the first item left on a touch device, asserts it disappears from the queue
- [ ] Create `frontend/e2e/feature-wt-late-join.spec.js` test that has the host at 50% of an episode, a guest joins the room, asserts the guest's player seeks to ~50% within 2s via `quix-join-room` `lastKnownTime` payload; then create `frontend/e2e/feature-wt-reactions.spec.js` (SEQ after late-join) that opens WatchTogether, taps the heart reaction, asserts a floating bubble appears on every participant's player for 2s, then fades
- [ ] Create `frontend/e2e/feature-smart-collections.spec.js` test that seeds IDB with 5 watched movies, 3 unwatched, 2 series-in-progress, asserts the 4 smart collection chips show correct counts and clicking filters
- [ ] Create `frontend/e2e/feature-mylist-collections.spec.js` test that creates 2 named lists, drags an item from list A to list B via DnD, shares list B, asserts share URL contains the list ID
- [ ] Create `frontend/e2e/feature-preferences.spec.js` test that opens PreferencesView, changes accent hue to 280, asserts `document.documentElement.style.getPropertyValue('--accent') === '280'`, reloads, asserts persistence
- [ ] Create `frontend/e2e/feature-auto-backup.spec.js` test that toggles auto-backup on, mocks `googleDriveService.uploadBackup`, fast-forwards 24h via fake timers, asserts `uploadBackup` was called once with the library JSON
- [ ] Create `frontend/e2e/feature-captions.spec.js` test that links a video with a `.vtt` subtitle URL, opens the captions menu, selects the track, asserts the `<track>` element is `mode="showing"` and subtitles are visible
- [ ] Create `frontend/e2e/feature-audio-lang.spec.js` test that links a video with multiple language tracks, opens the audio menu, switches from EN to IT, asserts the active `<audio>` track changes
- [ ] Create `frontend/e2e/feature-detail-extras.spec.js` test that opens a movie detail, switches to the Reviews tab (asserts reviews load), then Where to Watch (asserts providers), then More Like This (asserts recs load from TMDB cache)
- [ ] Create `frontend/e2e/feature-download.spec.js` test that clicks the download icon on a card, mocks `POST /api/fetch-blob`, asserts the blob is saved in the `downloadedBlobs` IDB table, goes offline, plays the item, asserts it plays from blob URL; then create `frontend/e2e/feature-download-progress.spec.js` (SEQ after download) that starts a download, asserts a progress bar appears, and asserts it reaches 100% then disappears
- [ ] Create `scripts/graphify-update.sh` (bash) that runs `bunx graphify export callflow-html && bunx graphify update . && git add graphify-out/` and commits nothing; then add `post-commit` git hook in `.husky/post-commit` that runs `stat -c %Y graphify-out/GRAPH_REPORT.md` and asserts the mtime is within 7 days, warning if stale; then add `"graphify:update": "bash scripts/graphify-update.sh"` and `"graphify:report": "bunx graphify export callflow-html"` to `package.json`; then create `frontend/e2e/invariants/no-stale-graphify.spec.js` test that asserts `graphify-out/GRAPH_REPORT.md` mtime is within 7 days, fails the build if stale — all in one sequential line
- [ ] Create `frontend/e2e/perf/lighthouse.spec.js` test that runs Lighthouse against the home and detail pages, asserts LCP < 2.5s, CLS < 0.1, INP < 200ms, and fails with a clear metrics report; then create `frontend/e2e/perf/bundle-size.spec.js` test that runs `bun run build`, reads `frontend/dist/assets/*.js`, asserts main chunk < 300KB gzipped and each lazy chunk < 100KB gzipped; then create `frontend/dist/.bundle-baseline.json` committed baseline from first run; then add lighthouse.spec.js as PR check posting LCP/CLS/INP metrics as comment; then fail build if main chunk grows >5% between runs compared against baseline — all in one sequential line
- [ ] Create `frontend/e2e/ui-bug-fixes.spec.js` test that visits home, asserts no React warning about invalid `sx` prop on `<main>`, asserts `<header>` is not duplicated, asserts `NotificationSnackbar` has `role="status"`
- [ ] Create `frontend/e2e/lazy-load.spec.js` test that opens home, asserts no `LinkEpisodesModal` chunk is in the initial network requests, clicks the link modal trigger, asserts the chunk loads and the modal mounts
- [ ] Create `frontend/e2e/slave-fullscreen.spec.js` test that starts playback on a slave, asserts `document.fullscreenElement` is set within 1s
- [ ] Create `frontend/e2e/modals-lazy.spec.js` test that opens home, asserts `document.querySelectorAll('[role="dialog"]').length === 0`, opens a modal, asserts it mounts
- [ ] Create `frontend/e2e/modals-focus-trap.spec.js` test that opens a modal, presses Tab repeatedly, asserts focus cycles within the modal and does not escape to the underlying page
- [ ] Create `frontend/e2e/sync-atomicity.spec.js` test that triggers `syncMediaFromMaster` with 100 items, kills the tab halfway via `page.close()` mid-loop, reopens, asserts the partial state was rolled back (no `cachedItems` orphans)
- [ ] Create `frontend/e2e/master-state-consistency.spec.js` test that sends a `play` command, then immediately receives `quix-slave-not-connected`, asserts `remoteStore.remoteSlaveState.isPlaying` reverts to false
- [ ] Create `frontend/e2e/store-media-refactor.spec.js` test that plays, pauses, seeks, stops, and navigates Home→Detail→Player, asserting all store actions still fire correctly after the mediaStore split
- [ ] Create `frontend/e2e/ws-resilience.spec.js` test that connects, kills backend for 5s, restarts, asserts auto-reconnect within 10s and no messages were lost (replay queue check)
- [ ] Create `frontend/e2e/ws-auth-required.spec.js` test that opens a WS without `Authorization` header, asserts server closes with code 4401, then opens with a valid JWT and asserts connection succeeds
- [ ] Create `frontend/e2e/ws-validation.spec.js` test that sends chat message with 1MB text (asserts reject), `command: 'rm -rf /'` (asserts reject), `slaveId: "a'; DROP TABLE--"` (asserts reject)
- [ ] Create `frontend/e2e/rate-limit.spec.js` test that opens a WS and sends 200 chat messages in 1 second, asserts that after the 5th the server returns `quix-rate-limited` and closes additional sends
- [ ] Create `frontend/e2e/heartbeat-fix.spec.js` test that opens WS, blocks network for 35s, asserts the server closes the socket within 5s after the heartbeat timeout fires
- [ ] Create `frontend/e2e/redis-state.spec.js` test that creates a room, restarts the backend container, asserts the room state is recovered from Redis and clients can re-join
- [ ] Create `frontend/e2e/ssrf-blocked.spec.js` test that POSTs to `/api/validate-link` with URLs `http://169.254.169.254/latest/meta-data/`, `http://10.0.0.1`, `http://192.168.1.1`, `http://127.0.0.1`, asserts all return 403, and `http://example.com` returns 200
- [ ] Create `frontend/e2e/caddy-tls.spec.js` test that navigates to `https://localhost` (Caddy), asserts the cert is valid and the page loads over HTTPS
- [ ] Create `frontend/e2e/no-root.spec.js` test that runs `docker exec stream-flix-app id`, asserts output starts with `uid=999`
- [ ] Create `frontend/e2e/google-auth-flow.spec.js` test that mocks the Google OAuth popup, asserts a JWT is returned from `POST /api/session`, stored in `db.preferences.sessionToken`, used in WS upgrade header, and removed on logout
- [ ] Create `frontend/e2e/api-tmdb-retry.spec.js` test that uses `page.route` to return 503 for the first 2 calls to TMDB, then 200, asserts the request succeeds and the store has the data
- [ ] Create `frontend/e2e/api-tmdb-cache.spec.js` test that calls the same TMDB endpoint twice, asserts only one network request goes out (cache hit on second)
- [ ] Create `frontend/e2e/link-validation.spec.js` test that adds a link with an invalid URL, asserts an error snackbar appears; adds a valid URL, asserts it persists
- [ ] Create `frontend/e2e/link-language-picker.spec.js` test that links a video with EN+IT tracks, opens the audio menu, switches to IT, asserts the active audio track changes
- [ ] Create `frontend/e2e/db-migration.spec.js` test that seeds IDB at schema v13, opens the app, asserts it migrates to v14 without data loss (asserts key items still present)
- [ ] Create `frontend/e2e/tv-i18n.spec.js` test that switches language to IT, asserts every text in `frontend/features/tv/` renders in Italian, switches to EN, asserts English
- [ ] Create `frontend/e2e/invariants/lint-passes.spec.js` test that runs `bunx eslint .` in a subprocess and asserts exit code 0, surfacing all lint errors
- [ ] Create `frontend/e2e/ci-workflow-valid.spec.js` test that reads `.github/workflows/*.yml` files, asserts each has required jobs (`lint`, `e2e`, `verify`), and YAML parses cleanly